# Rapport de veille SEO & production éditoriale — 2026-09-06

## Accès et bootstrap

Accès au dépôt `KhalidBougria/OptimasProtect_Blog` confirmé via le connecteur GitHub MCP local (`local_unpacked_qunfei-wu_github-mcp-server-js`), authentifié en tant que `KhalidBougria` (propriétaire du compte, et non `obougria` comme le prévoyait le prompt de référence — le connecteur de cette session est configuré directement sur le compte propriétaire). Permissions confirmées : `push: true`, `admin: true`. Visibilité du dépôt côté API : `public` (le prompt de référence la documente comme `privée` ; écart déjà noté par l'agent lors des runs précédents, sans conséquence sur l'accès).

`.seo/agent-prompt.md` existait déjà sur `main` (version 3.2) et a été lu intégralement avant toute action, conformément au bootstrap. Cette version de référence corrige deux points par rapport au texte reproduit dans la tâche planifiée elle-même : (1) la loi applicable est la **Loi n°27-06** relative aux activités de gardiennage et de transport de fonds (dahir n°1-07-155, 2007), et non « Loi n°35-09 » ; (2) la structure d'URL du blog est **`/articles/{slug}`**, et la marque s'écrit **OptimasProtect en un seul mot**, non « Optimas Protect » en deux mots ni « /blog/{slug} ». Conformément à la consigne de bootstrap (la version du dépôt prime sur le texte reproduit dans la tâche en cas de divergence), c'est cette version corrigée qui a été appliquée sur toute la durée de ce run. Le prompt de référence documente lui-même ces deux corrections comme arbitrées par l'utilisateur les 2026-08-22 et 2026-08-29.

`.seo/repo-map.md` (mis à jour 2026-09-05) et `.seo/backlog.md` (idem) étaient récents et n'ont pas nécessité de nouvelle cartographie complète ; leurs conventions ont été relues et appliquées telles quelles.

## Veille mots-clés et SERP

Rotation ciblée en priorité sur le pilier 2 (gestion des incidents / portail client), pilier structurellement sous la cible de ratio (§0) depuis plusieurs runs : `tableau de bord sécurité privée client`, `rapport mensuel gardiennage client`, `cahier de liaison sécurité privée`, `fiche incident agent de sécurité`, `logiciel gestion client société de sécurité`, `extranet client gardiennage`, `badge NFC ronde de sécurité`, `preuve de passage agent de sécurité`, `comment vérifier ronde agent de sécurité`, `société de sécurité privée preuve de service`, `modèle rapport d'incident agent de sécurité`, `temps de réponse agent de sécurité`, `comment prouver une ronde effectuée`, `vérification prestations société de sécurité`, `logiciel main courante gardiennage`, `solution digitale gardiennage Maroc`, `combien coûte un logiciel de ronde`, `logiciel de sécurité gardiennage avis`, `agent de sécurité qui dort pendant sa ronde`, `audit sécurité privée gardiennage Maroc` (autocomplete `gl=ma`, `hl=fr`).

Deux signaux exploitables sur 20 requêtes testées :
- **`fiche incident agent de sécurité`** / **`rapport d incident agent de sécurité`** → suggestions récurrentes `exemple rapport d'incident agent de sécurité pdf` et variantes (intention de recherche : modèle prêt à l'emploi). Relecture complète de l'article existant `rapport-intervention-ads-maroc` (PR #10, pilier 2, score 15/25) : l'intention recoupe largement celle déjà couverte (« rapport d'incident » et « rapport d'intervention » désignent le même document sur le terrain marocain). Décision anti-doublon (§0) : **enrichissement de l'article existant**, pas de nouvel article. Ajouts commités sur la branche `article/rapport-intervention-ads-maroc` : section de clarification terminologique (« rapport d'incident ou rapport d'intervention »), section « Modèle de rapport d'intervention prêt à remplir » (tableau à 8 champs, répondant directement à l'intention « exemple ... pdf »), et une 5e question FAQ sur l'équivalence des deux termes. Frontmatter mis à jour : `date` → 2026-09-06, ajout du champ `canonical` (absent auparavant, comme sur les autres articles). JSON-LD `Article` et `FAQPage` mis à jour en conséquence (`dateModified` ajouté, 5e Q/R ajoutée). Score inchangé (15/25), conformément au traitement déjà appliqué aux enrichissements précédents (PR #8, run du 2026-09-02).
- **`logiciel main courante gardiennage`** → `logiciel main courante sécurité`, `logiciel main courante sécurité gratuit`. Recoupe l'intention déjà couverte par PR #4 (`main-courante-electronique-maroc`) ; l'angle « gratuit » est trop proche pour justifier un article séparé sans risquer un doublon d'intention. Conservé en observation, pas d'action ce run.

Toutes les autres requêtes testées (16/20) : **0 suggestion**, confirmant une nouvelle fois le régime pauvre en autocomplete déjà documenté sur ce marché sur les runs précédents, y compris sur des angles pilier 2 jusque-là non testés.

`trends_interest` (`geo=MA`, 12 mois) sur `rapport d'incident agent de sécurité`, `rapport de ronde`, `cahier de consignes sécurité` : signal plat à 0 sur toute la période et toutes les régions testées — cohérent avec le comportement déjà documenté sur ce marché à faible volume absolu.

**Aucun sujet pilier 2 (ni pilier 1 ou 3) réellement neuf n'a passé le seuil de 14/25 pour un nouvel article ce run.** Conformément à la règle de bascule (§0) et à la logique d'anti-doublon, l'effort de production a été redirigé vers l'enrichissement d'un article pilier 2 existant plutôt que vers la création d'un article supplémentaire pilier 1 ou un doublon. L'écart de ratio pilier 2 (sous la cible de 30 %) reste donc non résorbé en nombre d'articles ce run, mais l'article pilier 2 existant a gagné en substance et en couverture sémantique.

## Veille concurrentielle

- **SEKUR Africa** : page Maroc (`sekur-africa.com/logiciel/logiciel-securite-privee-gardiennage-maroc/`) toujours en positionnement générique (planning multi-site, pointage géolocalisé, devis/contrats/factures, analyse de rentabilité par site), sans prix affiché sur cette page précise. La grille tarifaire européenne a pu être précisée ce run via une autre page : Pack Jeune Entreprise 69,99 € HT/mois, Pack Gestion 99,99 € HT/mois, Pack Terrain 9,99 € HT/mois, essai gratuit de 14 jours sans engagement — cohérent avec l'opacité tarifaire déjà documentée côté Maroc (aucun prix en MAD affiché). À noter pour comparaison future : ces paliers sont sensiblement différents de la fourchette précédemment relevée (59,99 à 29,99 €), signe possible d'une segmentation par taille d'entreprise plutôt que d'un changement de positionnement.
- **Trackforce Valiant** : toujours positionné à l'international, pricing sur devis, aucune adaptation Maroc identifiée. Confirmation supplémentaire par une source tierce (jobsquare.ma) que Trackforce Valiant et GuardTek sont utilisés comme systèmes de supervision de rondes au Maroc — cohérent avec le cercle 2 déjà documenté, aucune action nouvelle nécessaire.
- Aucun nouvel acteur marocain détecté ce run.

`site:optimasprotect.ma` reconfirmé à **0 résultat pertinent indexé** (recherche web) — seuls des homonymes sans rapport (Optimas Solutions, OptiRTC, etc.) remontent. Ce point est distinct de la structure d'URL déjà arbitrée (`/articles/{slug}`) : même si l'URL cible est connue, rien ne prouve que ce dépôt alimente réellement le site en ligne.

## Production

**1 enrichissement commité, 0 nouvel article.** Détail : voir section « Veille mots-clés » ci-dessus (branche `article/rapport-intervention-ads-maroc`, PR #10 existante, commit `6c2996a`). Aucune nouvelle branche ni nouvelle pull request créée ce run — la PR #10 existait déjà et reste en Phase 1 (relecture humaine), `draft: true` inchangé.

## Alertes

**Alerte haute (persistante, non résolue) — absence de générateur de site connecté au dépôt.** Aucun fichier de configuration de générateur de site statique (`astro.config.*`, `next.config.*`, `hugo.toml`, `package.json`, etc.) n'est présent dans ce dépôt, qui ne sert que de dépôt de contenu (Markdown) et de suivi (`.seo/`). Tant qu'un humain n'a pas branché un générateur de site sur ce dépôt (ou relié le générateur existant du site `optimasprotect.ma` à ce dépôt), aucun article produit par cet agent ne peut devenir une page en ligne, même après fusion d'une pull request. Ce point est documenté sans interruption depuis plusieurs runs.

**Alerte haute (persistante, non résolue) — 13 pull requests ouvertes, aucune fusionnée depuis le 2026-08-22**, soit plus de deux semaines sans relecture humaine. Répartition par pilier (avant l'enrichissement de ce run, qui ne modifie pas ce compte puisqu'aucune nouvelle PR n'a été créée) : pilier 1 = 9/14 PR (~64 %), pilier 2 = 3/14 (~21 %), pilier 3 = 2/14 (~14 %). Ce point est signalé sans interruption depuis le 2026-08-31 et continue de croître en ancienneté sans qu'aucune action humaine n'ait eu lieu. Non bloquant pour la veille et la production, mais à arbitrer par l'utilisateur : au-delà d'un certain volume, l'utilité de continuer à produire sans qu'aucune relecture n'ait lieu devrait être réévaluée.

**Point non bloquant — accès Search Console non configuré.** Comme indiqué au bootstrap, l'accès en lecture à `sc-domain:optimasprotect.ma` n'est pas encore configuré pour cette tâche planifiée. La veille s'est appuyée sur l'autocomplete et les tendances SERP observées, conformément à la clause de repli du §9.

**Point informatif — divergence entre le texte de la tâche planifiée et la version de référence du dépôt.** Le texte de bootstrap fourni à cette tâche planifiée reproduit une version antérieure du prompt de référence (mentionnant « Loi n°35-09 », des URLs en `/blog/{slug}` et « Optimas Protect » en deux mots). La version réelle sur le dépôt (`.seo/agent-prompt.md`, v3.2) a corrigé ces trois points début et fin août 2026. Cette exécution a suivi la version du dépôt, comme demandé par les instructions de bootstrap. Si le texte de la tâche planifiée elle-même doit un jour être resynchronisé avec le dépôt, c'est à l'utilisateur de le faire ; l'agent ne peut pas modifier sa propre définition de tâche planifiée depuis ce contexte.

## Moyen technique utilisé

Écriture sur GitHub effectuée via le **connecteur MCP GitHub local** (`local_unpacked_qunfei-wu_github-mcp-server-js`), pas d'appel direct à l'API GitHub nécessaire. Accès au dépôt fonctionnel de bout en bout ce run.

## Résumé chiffré

- Articles produits (nouveaux) : **0**
- Articles publiés (fusionnés) : **0**
- Articles mis à jour / enrichis : **1** (`rapport-intervention-ads-maroc`, PR #10)
- Pull requests ouvertes en attente de relecture : **13** (inchangé)
- Alertes hautes actives : **2** (générateur de site non connecté ; 13 PR sans relecture depuis plus de deux semaines)
