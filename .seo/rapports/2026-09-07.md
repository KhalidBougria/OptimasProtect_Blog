# Rapport de veille SEO & production éditoriale — 2026-09-07

## Accès et bootstrap

Accès au dépôt `KhalidBougria/OptimasProtect_Blog` confirmé via le connecteur GitHub MCP local (`local_unpacked_qunfei-wu_github-mcp-server-js`), authentifié en tant que `KhalidBougria` (propriétaire du compte, et non `obougria` comme le prévoyait le prompt de référence — le connecteur de cette session est configuré directement sur le compte propriétaire). Permissions confirmées : `push: true`, `admin: true`. Visibilité du dépôt côté API : `public` (le prompt de référence la documente comme « privée » ; écart déjà noté par l'agent lors des runs précédents, sans conséquence sur l'accès).

`.seo/agent-prompt.md` existait déjà sur `main` (version 3.2) et a été lu intégralement avant toute action, conformément au bootstrap. Cette version corrige trois points par rapport au texte reproduit dans la tâche planifiée elle-même : (1) la loi applicable est la **Loi n°27-06** relative aux activités de gardiennage et de transport de fonds (dahir n°1-07-155, 2007), et non « Loi n°35-09 » ; (2) la structure d'URL du blog est **`/articles/{slug}`**, et non `/blog/{slug}` ; (3) la marque s'écrit **« OptimasProtect » en un seul mot**, et non « Optimas Protect » en deux mots. Conformément à la consigne de bootstrap (la version du dépôt prime sur le texte reproduit dans la tâche en cas de divergence), c'est cette version corrigée qui a été appliquée sur toute la durée de ce run. `.seo/repo-map.md` (mis à jour 2026-09-05) et `.seo/backlog.md` (mis à jour 2026-09-06) étaient récents et n'ont pas nécessité de nouvelle cartographie complète ; leurs conventions ont été relues et appliquées telles quelles.

## Veille mots-clés et SERP

Rotation ciblée en priorité sur le pilier 2 (écart de ratio non résorbé depuis plusieurs runs), avec des angles pilier 1 et pilier 3 non testés précédemment. 15 requêtes testées en autocomplete (`gl=ma`, `hl=fr`) : `compte rendu de ronde de sécurité` (suggestions hors sujet, contexte québécois « SAAQ », aucun signal marocain exploitable), `suivi qualité prestation sécurité privée`, `rapport d'anomalie gardiennage`, `notification incident sécurité temps réel`, `traçabilité prestations société de sécurité`, `gestion réclamations société de sécurité`, `logiciel supervision gardiennage`, `avis client société de sécurité gardiennage`, `logiciel de contrôle de ronde Maroc`, `meilleure application de pointage agent de sécurité`, `gestion des congés agents de sécurité Maroc`, `taux de rotation agent de sécurité Maroc`, `carence agent de sécurité remplacement`, `absentéisme agent de sécurité solution`, `rapport activité mensuel société de sécurité`.

**Résultat : 0 suggestion exploitable sur les 15 requêtes.** Régime pauvre en autocomplete une nouvelle fois confirmé sur ce marché, y compris sur des angles jusque-là non testés.

`trends_interest` (`geo=MA`, 12 mois) sur `logiciel gardiennage Maroc`, `controle de ronde`, `pointage agent de securite` : plat à 0 sur toutes les régions testées, aucun signal `related_top` ni `related_rising`.

**Aucun sujet pilier 1, 2 ou 3 réellement neuf n'a passé le seuil de 14/25 ce run.** Les 10 clusters prioritaires du §4.2 ont désormais chacun au moins un article en PR ; aucun angle testé aujourd'hui n'apporte de signal ou d'intention suffisamment distincte pour justifier une création ou un enrichissement. Conformément à la règle « une journée sans article publié est normale, une journée sans veille ne l'est pas » (§4.1), la veille a été menée jusqu'au bout sans production.

## Veille concurrentielle

- **SEKUR** a publié une nouvelle page comparative datée 2026 : `sekur.fr/meilleurs-logiciels-securite-privee-2026/` (« Les 5 meilleurs logiciels pour agences de sécurité privée & de gardiennage en 2026 »), qui semble succéder à la version « 2025 » déjà documentée (`sekur-africa.com/meilleurs-logiciels-securite-privee-2025/`). Format à garder en tête comme idée de contenu futur pour le hub prix ou le pilier 2, avec la prudence habituelle sur la comparaison nominale (§8.2) — aucune action ce run. SEKUR Africa se présente par ailleurs comme née en 2024 (repositionnement) ; une mise à jour produit mineure (SEKUR Africa 4.31) a également été repérée, sans impact sur le positionnement ou les prix déjà documentés.
- **Trackforce Valiant** : positionnement international inchangé, pricing toujours sur devis, aucune adaptation Maroc identifiée.
- Aucun nouvel acteur marocain détecté.

`site:optimasprotect.ma` reconfirmé à **0 résultat pertinent indexé** (recherche web) — seuls des homonymes sans rapport (Optimas Solutions, OptiRTC, etc.) remontent.

## Production

**0 nouvel article, 0 enrichissement.** Aucune branche ni pull request créée ce run. Voir « Veille mots-clés et SERP » ci-dessus pour le détail de la décision anti-doublon / règle de bascule.

## Alertes

**Alerte haute (persistante, non résolue) — absence de générateur de site connecté au dépôt.** Aucun fichier de configuration de générateur de site statique (`astro.config.*`, `next.config.*`, `hugo.toml`, `package.json`, etc.) n'est présent dans ce dépôt, qui ne sert que de dépôt de contenu (Markdown) et de suivi (`.seo/`). Tant qu'un humain n'a pas branché un générateur de site sur ce dépôt (ou relié le générateur existant du site `optimasprotect.ma` à ce dépôt), aucun article produit par cet agent ne peut devenir une page en ligne, même après fusion d'une pull request. Ce point est documenté sans interruption depuis plusieurs runs.

**Alerte haute (persistante, non résolue) — 13 pull requests ouvertes, aucune fusionnée depuis le 2026-08-22**, soit désormais **16 jours** sans relecture humaine. Répartition inchangée ce run (aucune nouvelle PR) : pilier 1 = 9/13 (~69 %, au-dessus de la cible 60 % par effet de la règle de bascule), pilier 2 = 3/13 (~23 %, sous la cible 30 %), pilier 3 = 2/13 (~15 %, au-dessus de la cible 10 %). Combinée à l'alerte précédente, cela signifie qu'aucun des 13 articles déjà produits n'est à ce jour visible en ligne, même en cas de fusion immédiate de toutes les PR.

**Point non bloquant — accès Search Console non configuré.** Comme indiqué au bootstrap, l'accès en lecture à `sc-domain:optimasprotect.ma` n'est pas encore configuré pour cette tâche planifiée. La veille s'est appuyée sur l'autocomplete et les tendances SERP observées, conformément à la clause de repli du §9.

## Moyen technique utilisé

Écriture sur GitHub effectuée via le **connecteur MCP GitHub local** (`local_unpacked_qunfei-wu_github-mcp-server-js`), pas d'appel direct à l'API GitHub nécessaire. Accès au dépôt fonctionnel de bout en bout ce run (lecture et écriture confirmées).

## Résumé chiffré

- Articles produits (nouveaux) : **0**
- Articles publiés (fusionnés) : **0**
- Articles mis à jour / enrichis : **0**
- Pull requests ouvertes en attente de relecture : **13** (inchangé)
- Alertes hautes actives : **2** (générateur de site non connecté ; 13 PR sans relecture depuis 16 jours)
