# Rapport de veille — 2026-09-09

**Décision : 0 nouvel article, 0 enrichissement.** Journée de veille seule (§4.1 : « une journée sans article publié est normale, une journée sans veille ne l'est pas »).

## Bootstrap

Accès au dépôt reconfirmé : authentifié en tant que `KhalidBougria` (propriétaire du compte, et non `obougria` comme le prévoyait le prompt de référence), permissions `push: true` / `admin: true`. Voie technique : connecteur MCP GitHub local (`local_unpacked_qunfei-wu_github-mcp-server-js`).

`.seo/agent-prompt.md` (version 3.2) déjà présent sur `main` et relu en entier ce run — il prime sur le texte reproduit dans la tâche planifiée, qui reste une version obsolète sur trois points déjà corrigés dans le dépôt :
- marque : **OptimasProtect** en un seul mot (pas « Optimas Protect ») ;
- URL du blog : **`/articles/{slug}`** (pas `/blog/{slug}`) ;
- loi sectorielle : **Loi n°27-06** relative aux activités de gardiennage (dahir n°1-07-155, 2007), et non « Loi n°35-09 ».

À part : la **Loi n°32.26** (8h/jour pour les ADS, BO n°7526 du 16/07/2026) est un texte distinct, déjà traité en PR #12.

Search Console (`sc-domain:optimasprotect.ma`) toujours non configurée pour cette tâche planifiée — non bloquant, veille appuyée sur autocomplete/SERP/Trends comme prévu.

## Veille mots-clés

Rotation ciblée ce run sur des angles non testés lors des runs précédents, en priorité pilier 2 (écart de ratio non résorbé depuis plusieurs runs) :

- Pilier 2 : « portail client rapport incident sécurité », « main courante incident sécurité privée », « délai de réponse incident sécurité privée », « preuve intervention agent de sécurité », « historique interventions société de sécurité », « tableau de bord client sécurité privée », « alerte sécurité temps réel site client », « suivi ronde en temps réel client », « export rapport mensuel sécurité gardiennage », « logiciel gestion incident sécurité ».
- Pilier 1 : « digitaliser main courante gardiennage », « remplacer registre papier gardiennage », « badge NFC ronde de sécurité », « erreur de pointage agent de sécurité », « logiciel de sécurité gardiennage devis », « logiciel de ronde de sécurité sans matériel ».
- Pilier 3 : « turnover agent de sécurité Maroc solution ».

**0 suggestion sur la quasi-totalité de ces 17 requêtes** — régime pauvre en autocomplete une nouvelle fois confirmé sur ce marché. Deux signaux réels obtenus par élargissement (`expand`) :

1. **« rapport incident sécurité »** (via la graine « rapport incident gardiennage ») : suggestion réelle, plus « rapport d'incident de sécurité ». Anti-doublon vérifié : intention déjà couverte par la PR #10 (`rapport-intervention-ads-maroc`, pilier 2), enrichie le 2026-09-06 avec précisément le synonyme « rapport d'incident » et un modèle prêt à remplir. **Pas de nouvel article, pas de mise à jour nécessaire.**
2. **« gestion de conflit agent de sécurité »** / « gestion des conflits agent de sécurité » / « formation gestion de crise agent de sécurité » : signal réel mais intention formation/conduite du personnel, pas achat logiciel — exclu par la règle §4.3.2 (têtes de requête dominées par l'intention emploi/formation). **Non retenu.**

`trends_interest` (`geo=MA`, 12 mois) sur « rapport incident sécurité », « logiciel gardiennage Maroc », « controle de ronde » : plat à 0 sur toutes les régions testées, aucune série exploitable — cohérent avec les runs précédents.

## Veille concurrentielle

- **SEKUR / SEKUR Africa** : tarification toujours en EUR uniquement pour l'offre France (à partir de 99,99 €/mois pour 6 utilisateurs, sans frais d'installation, essai gratuit 14 jours). Opacité tarifaire MAD côté Maroc toujours confirmée. Aucun changement de positionnement détecté.
- **Trackforce Valiant** : toujours positionné à l'international, pricing sur devis, aucune adaptation Maroc identifiée.
- **Nouveaux acteurs détectés** : `secumall.ma` et `kver.ma`, revendeurs marocains de contrôleurs de ronde **physiques** (boîtiers Rondier/RFID). Il s'agit de matériel, pas de logiciel SaaS — cercle 3 (substituts matériels, non surveillables en continu). Ce constat renforce plutôt le différenciateur « sans matériel dédié » d'Optimas ; à garder en tête comme illustration concrète possible dans un futur article comparatif, sans dénigrement nominal (§8.2).
- **GRS Maroc** : société de gardiennage utilisant elle-même la technologie NFC en interne pour ses propres agents — c'est un client potentiel, pas un éditeur concurrent.
- `site:optimasprotect.ma` : toujours 0 résultat pertinent indexé (recherche web).

## Vérification structurelle du dépôt

Racine de la branche `main` recontrôlée ce run : elle ne contient **que** le dossier `.seo/` (aucun `content/blog`, aucun fichier de configuration de générateur, aucun `.github/workflows`). Ceci confirme qu'aucune page n'est actuellement publiée en live, et que le mécanisme de force-push vers `main` mentionné dans le commentaire historique de la PR #9 (2026-08-26) n'est pas actif sur `main` dans son état constaté ce jour — point à resurveiller si un pipeline est branché ultérieurement.

## Bilan des pull requests

**13 PR ouvertes, 0 fusionnée**, depuis le 2026-08-22 — soit **18 jours** sans relecture humaine à ce jour. Ce point est signalé sans interruption depuis le 2026-08-31 et continue de croître en ancienneté sans qu'aucune action humaine n'ait eu lieu.

Répartition par pilier (inchangée ce run, aucune nouvelle PR) : pilier 1 = 9/13 (~69 %, au-dessus de la cible 60 %, effet de la règle de bascule faute d'alternative pilier 2/3 qualifiée), pilier 2 = 3/13 (~23 %, sous la cible 30 %), pilier 3 = 2/13 (~15 %, au-dessus de la cible 10 %).

## Alertes hautes

1. **Aucun générateur de site ni pipeline de build/déploiement connecté à ce dépôt** — alerte permanente, aucun article produit par cet agent ne peut devenir une page live tant qu'un humain n'a pas branché un générateur de site statique sur ce dépôt (ou relié le générateur existant du site `optimasprotect.ma` à ce dépôt).
2. **13 PR ouvertes, aucune fusionnée depuis 18 jours** — la Phase 1 (relecture humaine) fonctionne comme prévu techniquement, mais le volume croissant sans relecture mérite un arbitrage humain.
3. `robots.txt` du site toujours hors du périmètre autorisé pour l'outil de fetch de cette session — non vérifié directement.
4. `site:optimasprotect.ma` toujours à 0 résultat pertinent indexé.
5. Search Console non configurée pour cette tâche planifiée (rappelé au bootstrap, non bloquant).

## Résumé pour la synthèse hebdomadaire (lundi prochain)

0 article produit cette semaine à ce jour (dernier article : PR #15 le 2026-09-05). Écart de ratio pilier 2 non résorbé. Prochaine session : traiter en priorité absolue un sujet pilier 2 réellement neuf si le seuil de 14/25 est atteint en veille.
