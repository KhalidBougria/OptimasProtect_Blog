# Rapport de veille SEO & production éditoriale — 2026-09-08

## Accès et bootstrap

Accès au dépôt `KhalidBougria/OptimasProtect_Blog` confirmé via le connecteur GitHub MCP local (`local_unpacked_qunfei-wu_github-mcp-server-js`), authentifié en tant que `KhalidBougria` (propriétaire du compte, et non `obougria` comme le prévoyait le prompt de référence — le connecteur de cette session est configuré directement sur le compte propriétaire). Permissions confirmées : `push: true`, `admin: true`. Visibilité du dépôt côté API : `public` (le prompt de référence la documente comme « privée » ; écart déjà noté par l'agent lors des runs précédents, sans conséquence sur l'accès).

`.seo/agent-prompt.md` existait déjà sur `main` (version 3.2) et a été lu intégralement avant toute action, conformément au bootstrap. Cette version corrige trois points par rapport au texte reproduit dans la tâche planifiée elle-même : (1) la loi applicable est la **Loi n°27-06** relative aux activités de gardiennage et de transport de fonds (dahir n°1-07-155, 2007), et non « Loi n°35-09 » ; (2) la structure d'URL du blog est **`/articles/{slug}`**, et non `/blog/{slug}` ; (3) la marque s'écrit **« OptimasProtect » en un seul mot**, et non « Optimas Protect » en deux mots. Conformément à la consigne de bootstrap (la version du dépôt prime sur le texte reproduit dans la tâche en cas de divergence), c'est cette version corrigée qui a été appliquée sur toute la durée de ce run. `.seo/repo-map.md` (mis à jour 2026-09-05) et `.seo/backlog.md` (mis à jour 2026-09-07) étaient récents et n'ont pas nécessité de nouvelle cartographie complète ; leurs conventions ont été relues et appliquées telles quelles.

## Veille mots-clés et SERP

Rotation ciblée sur des angles non essayés lors des runs précédents (voir `.seo/backlog.md` pour l'historique complet des requêtes déjà testées), tous piliers confondus :

- **Pilier 2** : `logiciel gestion incidents sécurité privée`, `alerte panique agent de sécurité`, `communication client société de sécurité`, `consultation rapport ronde client en ligne` — 0 suggestion sur les 4.
- **Pilier 3** : `planning agents de sécurité logiciel`, `remplacement agent de sécurité absence`, `gestion heures agent de sécurité Maroc`, `contrat de travail agent de sécurité Maroc`, `évaluation performance agent de sécurité - 1 signal (voir ci-dessous), 0 sur les 4 autres.
- **Pilier 1** : `digitaliser rondes de sécurité Maroc`, `alternative pointeuse biométrique gardiennage`, `logiciel conforme cahier des charges gardiennage`, `effectif agent de sécurité minimum site`, `consigne de sécurité gardiennage modèle`, `gestion multi-sites société de sécurité`, `logiciel de ronde sans matériel dédié`, `essai gratuit logiciel gardiennage`, `comparatif logiciel gardiennage Maroc` - 0 sur les 9.
- **Angles sectoriels** (nouveaux) : `gardiennage site industriel Maroc`, `sécurité centre commercial Maroc`, `gardiennage chantier Maroc`, `gardiennage résidence Maroc logiciel`, `cahier des charges type gardiennage Maroc` - 0 sur 4, sauf `appel d'offre gardiennage Maroc` (voir ci-dessous).

**Résultat : 2 signaux exploitables sur 22 requêtes testées, tous deux déjà couverts par des PR existantes.**

- `planning agents de sécurité logiciel` → suggestion `logiciel planning agent de sécurité gratuit`. Vérification du contenu de PR #6 (`planning-agents-securite-maroc`) : la FAQ de cet article répond déjà directement à « Un logiciel de planning gratuit suffit-il pour une petite société de sécurité ? ». Intention déjà couverte, aucune mise à jour nécessaire.
- `appel d'offre gardiennage Maroc` → 5 suggestions (« appel d'offre gardiennage maroc 2025 », « … 2026 », « appel d offre sécurité gardiennage maroc »). Signal réel, mais recoupe directement l'intention de PR #8 (`cahier-des-charges-gardiennage-maroc`, hub conformité, cluster 7), qui contient déjà une section dédiée et une checklist « avant de répondre à un appel d'offres de gardiennage ». Intention déjà couverte en profondeur, aucune mise à jour nécessaire.

`trends_interest` (`geo=MA`, 12 mois) sur `logiciel de ronde`, `digitalisation gardiennage`, `pointage agent sécurité` : aucune série temporelle exploitable (`interest_over_time` vide), un seul pic isolé sur une région (Tadla-Azilal, valeur 100, toutes les autres régions testées à 0) — signal trop ponctuel et non corroboré pour être retenu.

**Aucun sujet pilier 1, 2 ou 3 réellement neuf n'a passé le seuil de 14/25 ce run.** Conformément à la logique anti-doublon et à la règle « une journée sans article publié est normale, une journée sans veille ne l'est pas » (§4.1), aucune création ni aucun enrichissement n'a été retenu.

## Veille concurrentielle

- **SEKUR / SEKUR Africa** : page comparative `sekur.fr/meilleurs-logiciels-securite-privee-2026/` toujours en ligne (déjà documentée), tarification EUR uniquement pour l'offre France, opacité tarifaire MAD côté Maroc toujours confirmée. Aucun changement de positionnement détecté depuis le run précédent.
- **Trackforce Valiant** : positionnement international inchangé (300 000+ professionnels, 30 000+ sites, 45 pays), pricing toujours sur devis, aucune adaptation Maroc identifiée.
- Aucun nouvel acteur marocain détecté.
- `site:optimasprotect.ma` reconfirmé à **0 résultat pertinent indexé** (recherche web) — seuls des homonymes sans rapport (Optimas Solutions, OptiRTC, etc.) remontent.

## Production

**0 nouvel article, 0 enrichissement.** Aucune branche ni pull request craéée ce run. Voir « Veille mots-clés et SERP » ci-dessus pour le détail de la décision anti-doublon.

## Alertes

**Alerte haute (persistante, non résolue) — absence de générateur de site connecté au dépôt.** Aucun fichier de configuration de générateur de site statique (`astro.config.*`, `next.config.*`, `hugo.toml`, `package.json`, etc.) n'est présent dans ce dépôt, qui ne sert que de dépôt de contenu (Markdown) et de suivi (`.seo/`). Tant qu'un humain n'a pas branché un générateur de site sur ce dépôt (ou relié le générateur existant du site `optimasprotect.ma` à ce dépôt), aucun article produit par cet agent ne peut devenir une page en ligne, même après fusion d'une pull request. Ce point est documenté sans interruption depuis plusieurs runs.

**Alerte haute (persistante, non résolue) — 13 pull requests ouvertes, aucune fusionnée depuis le 2026-08-22**, soit désormais **17 jours** sans relecture humaine. Ce point est signalé sans interruption depuis le 2026-08-31 et continue de croître en ancienneté sans qu'aucune action humaine n'ait eu lieu. Répartition par pilier inchangée ce run (aucune nouvelle PR) : pilier 1 = 9/13 (~69 %, au-dessus de la cible 60 % par effet de la règle de bascule), pilier 2 = 3/13 (~23 %, sous la cible 30 %), pilier 3 = 2/13 (~15 %, au-dessus de la cible 10 %). Combinée à l'alerte précédente, cela signifie qu'aucun des 13 articles déjà produits n'est à ce jour visible en ligne, même en cas de fusion immédiate de toutes les PR.

**Point non bloquant — accès Search Console non configuré.** Comme indiqué au bootstrap, l'accès en lecture à `sc-domain:optimasprotect.ma` n'est pas encore configuré pour cette tâche planifiée. La veille s'est appuyée sur l'autocomplete et les tendances SERP observées, conformément à la clause de repli du §9.

## Moyen technique utilisé

Écriture sur GitHub effectuée via le **connecteur MCP GitHub local** (`local_unpacked_qunfei-wu_github-mcp-server-js`), pas d'appel direct à l'API GitHub nécessaire. Accès au dépôt fonctionnel de bout en bout ce run (lecture et écriture confirmées).

## Résumé chiffré

- Articles produits (nouveaux) : **0**
- Articles publiés (fusionnés) : **0**
- Articles mis à jour / enrichis : **0**
- Pull requests ouvertes en attente de relecture : **13** (inchangé)
- Alertes hautes actives : **2** (générateur de site non connecté ; 13 PR sans relecture depuis 17 jours)
