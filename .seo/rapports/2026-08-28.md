# Rapport de veille SEO & production éditoriale — Optimas Protect
## 2026-08-28

## Bootstrap

- Accès au dépôt `KhalidBougria/OptimasProtect_Blog` confirmé via le connecteur GitHub MCP local (`local_unpacked_qunfei-wu_github-mcp-server-js`), authentifié en tant que `KhalidBougria` (propriétaire du compte), permissions `push: true` / `admin: true`. Pas d'appel direct à l'API GitHub hors MCP.
- `.seo/agent-prompt.md` déjà présent sur `main` (version 3.1) : lu en entier et utilisé comme référence. Cette version corrige une erreur de la version précédente — la loi régissant les activités privées de sécurité au Maroc est la **Loi n°27-06** (dahir n°1-07-155, 2007), et non « Loi n°35-09 » — correction confirmée par l'utilisateur le 2026-08-22 et déjà répercutée dans tout le contenu produit depuis.
- `.seo/repo-map.md` déjà présent : lu, puis mis à jour ce run (condensé, alerte workflow reconfirmée close).
- Search Console non configurée pour ce domaine — veille appuyée sur autocomplete (gl=ma, hl=fr) et Google Trends (geo=MA), conformément au §9.

## Décision du jour : pause de production

Semaine S35 (24–30 août) : 5 articles déjà produits du lundi au jeudi (`planning-agents-securite-maroc` et `modele-rapport-de-ronde-maroc` le 24, `cahier-des-charges-gardiennage-maroc` le 25, `application-pointage-ads-maroc` le 26, `rapport-intervention-ads-maroc` le 27). La cadence cible du §9 (2 à 3 articles/semaine en phase de lancement) est déjà dépassée. Conformément à « qualité avant quota » (§8.4) et à la règle du §4.1 (« une journée sans article publié est normale, une journée sans veille ne l'est pas »), **aucun nouvel article n'a été produit ce run**. Veille complète réalisée à la place (étapes 1, 2, 3 et 6 du §4.1).

## Veille mots-clés et SERP

Outils utilisés : autocomplete Google (gl=ma, hl=fr) et Google Trends (geo=MA). Résultat consistant avec les runs précédents : le marché francophone marocain du logiciel de gardiennage reste structurellement pauvre en autocomplete (0 suggestion) sur la quasi-totalité des requêtes testées, y compris des requêtes centrales comme « logiciel gardiennage Maroc » ou « prix logiciel sécurité privée » — un contrôle sur une requête générique (« restaurant ») confirme que l'outil fonctionne normalement, ce qui exclut un problème technique et confirme qu'il s'agit d'un signal réel de marché jeune (cohérent avec le §4.1 : l'autocomplete est structurellement pauvre ici, ce n'est pas un échec de veille). Requêtes testées spécifiquement pour les deux sujets en file d'attente :

| Requête | Autocomplete (gl=ma) | Trends (geo=MA, 12 mois) |
|---|---|---|
| faux pointage agent de sécurité | 0 suggestion | — |
| détecter faux pointage agent de sécurité (rappel run précédent) | 0 suggestion | — |
| obligations loi 27-06 employeur | 0 suggestion | — |
| logiciel gardiennage Maroc | 0 suggestion (y compris expansion alphabet) | — |
| main courante numérique gardiennage | 0 suggestion | — |
| portail client transparence prestations sécurité | 0 suggestion | — |
| contrôle de ronde NFC | — | quasi nul (un seul pic isolé à 100 la semaine du 3 mai, sinon 0) |
| logiciel gardiennage / pointage ADS / cahier des charges gardiennage (groupé) | — | 0 sur les 12 derniers mois |

## Veille concurrentielle

- **SEKUR Africa** : la page dédiée Maroc ne publie toujours aucun prix inline et renvoie vers une page `/tarifs/` globale, non localisée. Cette page affiche des prix en **euros uniquement** (59,99 € HT/mois pour 10 licences, dégressif selon engagement jusqu'à 29,99 € HT/mois à 36 mois ; licence supplémentaire 0,99 € à 0,49 € HT/mois). Aucune déclinaison en dirhams. Ceci confirme et actualise l'opacité tarifaire déjà documentée sur ce marché — un point d'appui éditorial réel pour Optimas Protect (prix MAD affichés publiquement, conversion journalière).
- `site:optimasprotect.ma` : toujours 0 résultat indexé sur Google (reconfirmé).

## Sujets scorés (seuil de production : 14/25)

| Sujet | Pilier | Intention | Faisabilité | Avantage local | Volume | Fraîcheur | Score |
|---|---|---|---|---|---|---|---|
| Obligations de la Loi n°27-06 pour l'employeur (cluster 8, hub conformité) | 1 (régime renforcé) | 4 | 4 | 5 | 2 | 3 | **18/25** |
| Faux pointage ADS : comment le détecter sans accuser ses agents (cluster 9) | 1/2 croisé | 4 | 3 | 4 | 2 | 3 | **16/25** |

Les deux sujets passent le seuil et sont consignés en file d'attente dans `.seo/backlog.md`, prêts pour la prochaine journée de production, priorité au premier (complète le hub conformité qui n'a qu'un seul article à ce jour).

## Ratio hebdomadaire (§0 — cible 60/30/10)

Sur les 9 articles en PR ouverte à ce jour : pilier 1 = 5/9 (≈56 %), pilier 2 = 3/9 (≈33 %), pilier 3 = 1/9 (≈11 %). Proche de la cible ; pas de bascule nécessaire.

## État de publication

Aucun changement aujourd'hui : les 9 PR d'articles (#1, #2, #4 à #10) restent **ouvertes, non mergées**, en Phase 1 conformément à la consigne de la tâche planifiée. Aucun commit direct sur `main` en dehors des fichiers `.seo/` (backlog, repo-map, ce rapport).

## Alertes

**Alerte critique du 2026-08-26 (workflow `copie-repo` contournant la Phase 1) — reconfirmée résolue.** `list_workflows` renvoie `total_count: 0` (aucun workflow GitHub Actions dans le dépôt). Le listing de la racine de `main` confirme une arborescence strictement limitée à `.seo/` — les 7 fichiers d'articles précédemment force-poussés sur `main` avant la suppression du workflow ont été retirés le 2026-08-27. Point considéré clos.

**Alertes non résolues (arbitrage humain toujours nécessaire), inchangées depuis le dernier contrôle (2026-08-25) :**
1. Le dépôt Git ne semble pas connecté au site live `optimasprotect.ma` (aucune section `/blog/` visible, assets du site pointant vers un outil tiers type GPT Engineer / Lovable). Aucun article produit par cet agent ne peut donc devenir consultable sans intervention humaine.
2. Écart d'orthographe de marque : le site live affiche systématiquement « OptImasProtect » (un mot) alors que la charte de cet agent impose « Optimas Protect » (deux mots). L'agent continue d'appliquer la règle du prompt dans les articles produits, en attendant arbitrage.
3. `robots.txt` / accessibilité Googlebot non revérifiés (URL bloquée par l'outil de fetch de cette session au dernier essai).

## Moyen technique utilisé

Connecteur GitHub MCP local (lecture et écriture), aucun appel direct à l'API GitHub hors MCP.

## Résumé chiffré

Articles produits ce run : **0**. Articles publiés (mergés) : **0** (les 9 PR restent en attente de relecture humaine). Articles mis à jour : **0**. Alertes hautes actives : **3** (connexion site live, écart de marque, robots.txt non revérifié) — aucune nouvelle alerte haute ce run.
