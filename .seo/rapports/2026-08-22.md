# Rapport de veille SEO & production éditoriale — 2026-08-22

Premier run de l'agent (bootstrap complet). Dépôt `KhalidBougria/OptimasProtect_Blog` entièrement vide au démarrage : 0 commit, 0 branche.

## Alertes hautes

1. **Site optimasprotect.ma non vérifiable.** `https://optimasprotect.ma/` renvoie un contenu vide au fetch (deux tentatives), zéro résultat indexé sur `site:optimasprotect.ma`. Impossible de contrôler `robots.txt`, le rendu statique vs SPA, ou l'existence effective d'un `/blog/` public. Combiné à l'absence de générateur de site dans le dépôt (voir alerte 3), il est probable que le site public n'est pas encore branché sur ce dépôt ou n'est pas en ligne. **Action humaine requise avant toute attente de résultat SEO.**
2. **Écart factuel sur la loi citée dans le prompt de référence.** Le prompt cite systématiquement la « Loi n°35-09 » comme texte régissant la sécurité privée au Maroc. La recherche de ce run (Bulletin Officiel, SGG, cabinets juridiques marocains) converge vers la **loi n° 27-06** (dahir n° 1-07-155, 2007) comme texte réellement en vigueur sur le gardiennage et le transport de fonds ; aucune source ne confirme l'existence d'une « loi 35-09 » sur ce sujet. Par prudence (§8.3), **aucun contenu réglementaire n'a été produit ce run**, le cluster 8 et le hub conformité restent bloqués jusqu'à vérification humaine. Détail dans `.seo/repo-map.md`.
3. **Aucun générateur de site dans le dépôt.** Le dépôt ne contenait aucun fichier avant ce run (pas d'Astro/Next/Hugo/Gatsby, pas de `package.json`). Des conventions de contenu ont été établies par cet agent (`content/blog/{slug}.md`, frontmatter documenté dans `.seo/repo-map.md`) mais aucun des articles produits ne peut devenir une page live tant qu'un humain n'a pas branché un pipeline de build/déploiement sur ce dépôt (ou pointé le générateur existant du site vers ce dépôt, s'il existe ailleurs).

## Accès et méthode technique

- **Accès au dépôt :** fonctionnel. Authentifié en tant que `KhalidBougria` (propriétaire du compte), et non `obougria` comme prévu au prompt de référence — probablement une configuration du connecteur sur le compte propriétaire directement. Permissions confirmées : lecture/écriture complètes (`push: true`).
- **Moyen technique d'écriture :** connecteur MCP GitHub local (outils `mcp__local_unpacked_qunfei-wu_github-mcp-server-js__*`), pas d'appel direct à l'API GitHub hors MCP.
- **Search Console :** non configurée pour cette tâche planifiée (confirmé par un appel `gsc_query` qui retourne une erreur 403 « insufficient permission »), conformément au bootstrap. Veille appuyée sur autocomplete Google (`gl=ma`, `hl=fr`) et recherche web.

## Bootstrap effectué

- Création de `.seo/agent-prompt.md` (commit direct sur `main`, premier commit du dépôt).
- Création de `.seo/repo-map.md` : cartographie du dépôt vide, conventions de contenu établies, URLs de veille concurrentielle consignées, alertes 1 et 2 documentées.

## Veille mots-clés et concurrents

Autocomplete Google (Maroc, français) sur les graines : « logiciel gardiennage Maroc » (aucune suggestion), « contrôle de ronde » (confirme « contrôleur de ronde maroc », « contrôleur de ronde prix maroc »), « pointage ADS » (bruit hors sujet, terme trop ambigu en anglophone/générique, à éviter tel quel), « rapport de ronde » (volume confirmé, essentiellement Québec/France, angle Maroc à construire), « prix logiciel sécurité privée » (aucune suggestion, confirme l'opacité tarifaire du marché), « cahier des charges gardiennage » (confirme « cahier des charges sécurité et gardiennage maroc »). Google Trends Maroc sur ces mêmes graines : signal trop faible pour être exploitable (marché encore jeune), conforme à l'attente du prompt de référence.

Concurrents (cercle 1 et 2) : SEKUR Africa (Maroc, pas de prix public constaté) et Trackforce Valiant (international, prix sur devis confirmé par plusieurs comparateurs tiers). Aucun nouvel acteur marocain non identifié détecté. URLs consignées dans `.seo/repo-map.md`.

## Scoring et sélection

| Sujet | Pilier | Intention (5) | Faisabilité rank (5) | Avantage local (5) | Volume (5) | Fraîcheur (5) | Total /25 |
|---|---|---|---|---|---|---|---|
| Contrôle de ronde NFC au Maroc : comment ça marche | 1 | 5 | 4 | 5 | 3 | 5 | **22** |
| Prix d'un logiciel de gardiennage au Maroc en 2026 | 1 | 5 | 4 | 5 | 3 | 5 | **22** |

Les deux sujets dépassent largement le seuil de 14/25 et ont été retenus en priorité comme premiers articles des deux hubs pilier 1 (hub traçabilité, hub prix), conformément à la logique de bootstrap du dépôt (le premier article devient la référence, §7.1 point 5).

Aucun sujet pilier 2 ou pilier 3 n'a été scoré ce run : l'intégralité du temps de production a été consacrée à établir les deux hubs pilier 1 fondateurs, ce qui est cohérent avec la règle de bascule du §0 (priorité pilier 1 tant que le dépôt est vide) mais laisse le ratio hebdomadaire 60/30/10 entièrement du côté pilier 1 pour l'instant. À rééquilibrer sur les prochains runs de la semaine.

## Compteur hebdomadaire du ratio (semaine du 17 au 23 août 2026)

- Pilier 1 : 2 articles produits (100 %)
- Pilier 2 : 0 article (0 %)
- Pilier 3 : 0 article (0 %)

## Production

- **2 articles produits et déposés en pull request** (Phase 1, `draft: true`), aucun publié (voir alerte 3 : pas de build possible de toute façon à ce stade) :
  1. `controle-de-ronde-nfc-gardiennage-maroc` — PR #1 — https://github.com/KhalidBougria/OptimasProtect_Blog/pull/1
  2. `prix-logiciel-gardiennage-maroc` — PR #2 — https://github.com/KhalidBougria/OptimasProtect_Blog/pull/2
- 0 article mis à jour (aucun contenu existant, anti-doublon non applicable ce run).

## File d'attente (voir `.seo/backlog.md`)

Sujets identifiés pour les prochains runs, dont plusieurs pilier 2 et 3 pour rééquilibrer le ratio hebdomadaire.
