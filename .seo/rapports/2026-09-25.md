# Rapport quotidien — 2026-09-25

## Bootstrap

Acces au depot `KhalidBougria/OptimasProtect_Blog` verifie et fonctionnel via le connecteur GitHub MCP (`mcp__remote-devices__GitHub_MCP_Server__JS___*`), authentifie en tant que **KhalidBougria** (proprietaire du compte, pas `obougria` comme prevu au prompt de reference — ecart deja documente depuis les premiers runs). Permissions confirmees : `push: true`, `admin: true`. Visibilite du depot cote API : **public** (le prompt de reference et son tableau §7 indiquent « prive » — ecart deja connu, non actionnable par cet agent).

Le bac a sable Bash de cette session est fonctionnel localement (encodage/decodage, verification d'integrite octet par octet), mais le reseau externe y est restreint : `curl` echoue en tunnel proxy (403) sur `optimasprotect.ma` et `raw.githubusercontent.com`, hors liste blanche. `api.github.com` repond correctement au niveau reseau, mais un appel direct sur le contenu de ce depot precis est refuse (« GitHub access to this repository is not enabled for this session, add_repo required »), identique aux runs precedents. Le connecteur MCP GitHub reste donc la voie technique utilisee pour toutes les ecritures de ce run, conformement au bootstrap.

`.seo/agent-prompt.md` (version 3.2) relu integralement : a jour, aucune divergence avec les corrections deja actees (Loi n°27-06, URLs `/articles/{slug}`, marque **OptimasProtect** en un seul mot). Ces corrections priment sur le texte reproduit dans le corps de la tache planifiee elle-meme, qui diverge encore sur ces trois points — conformement aux instructions de bootstrap, la version du depot fait foi et a ete appliquee integralement ce run.

`.seo/repo-map.md` non relu integralement ce run (pas de modification necessaire, aucune alerte nouvelle a y consigner ; se referer aux rapports du 2026-09-12 au 2026-09-24 pour le detail des alertes ouvertes, reportees telles quelles).

## Veille mots-cles et SERP

Marche cible : Maroc (`gl=ma`, `hl=fr`). 16 requetes testees ce run, sur des angles non essayes lors des runs precedents (voir historique dans `.seo/backlog.md`) : logiciel de ronde de nuit gardiennage Maroc, controle a distance des rondes de securite, systeme de pointage sans pointeuse agent de securite, solution de tracabilite gardiennage Maroc, logiciel de securite privee sans materiel dedie, deploiement rapide logiciel de gardiennage, combien de temps pour installer un logiciel de gardiennage, difference entre pointeuse et logiciel de ronde NFC, logiciel de suivi des agents de securite sans GPS, budget logiciel de gardiennage Maroc, gestion des equipes de securite multi-clients, logiciel RH gardiennage Maroc, gestion des heures supplementaires agents de securite Maroc, controle d'assiduite agents de securite Maroc, modele de rapport d'incident securite privee, logiciel de gestion de site gardiennage Maroc.

**0 suggestion sur 16** — shutout total, au moins aussi faible que le record deja documente du 2026-09-24 (0/16). Regime pauvre en autocomplete confirme avec une intensite maximale ce run.

`trends_interest` (geo=MA, 12 mois) sur logiciel gardiennage Maroc / controle de ronde / pointage agent securite : interet a 0 sur toute la periode sauf un pic isole a 100 la semaine du 2026-06-21 sur « pointage agent securite », deja ancien et sans serie associee — signal non actionnable, coherent avec le regime pauvre observe depuis plusieurs mois.

Aucun sujet reellement neuf (pilier 1, 2 ou 3) n'a franchi le seuil de production de 14/25 ce run.

### Search Console

Nouvel appel a `gsc_query` sur `sc-domain:optimasprotect.ma` (`country=mar`, 2026-08-26 a 2026-09-23, row_limit=50) : **erreur 403**, « User does not have sufficient permission for site 'sc-domain:optimasprotect.ma' ». Identique au test du 2026-09-24. Aucune action possible cote agent : necessite qu'un acces en lecture soit accorde cote Google Search Console. Conformement au bootstrap, la veille continue de s'appuyer sur les positions SERP observees et l'autocomplete en attendant.

## Veille concurrentielle

Recherche web ciblee sur de nouveaux entrants marocains : ne remonte que des acteurs deja documentes (SEKUR Africa, EasyGard/SGGI, IBE Maroc/Guard Online) ainsi que des resultats generiques hors cible (fournisseurs France/Europe, sites d'agregation B2B). Aucun nouvel acteur marocain detecte.

SEKUR Africa, EasyGard/SGGI, Trackforce Valiant : non reverifies en detail ce run (dernieres verifications du 2026-09-16 au 2026-09-24 jugees suffisamment recentes), aucun changement attendu ni detecte incidemment.

## Decision de production

**Aucun sujet n'a franchi le seuil de 14/25 ce run** (0 suggestion exploitable sur 16 requetes, resultat au moins aussi faible que le pire de la serie documentee). **Aucun nouvel article produit ni mis a jour.** Conforme a la regle qu'une journee sans article publie est normale, une journee sans veille ne l'est pas (§4.1), et a la priorite qualite avant quota (§8.4).

Repartition par pilier inchangee depuis le 2026-09-11 : pilier 1 = 8/14 (~57 %), pilier 2 = 4/14 (~29 %), pilier 3 = 2/14 (~14 %), proche de la cible 60/30/10.

## Etat du depot et file de pull requests

14 branches `article/*` et 14 PR ouvertes reconfirmees (numeros 1, 2, 4, 5, 6, 7, 8, 9, 10, 12, 13, 14, 15, 16) : **aucune fusion depuis le 2026-08-29** (premiere PR d'article), soit **27 jours** au 2026-09-25 ; aucune nouvelle PR depuis le 2026-08-22, soit **34 jours**. Seules les PR 3 et 11 (corrections du prompt de reference) sont fusionnees.

Verification technique du site live via WebFetch (curl bloque sur ce domaine, cf note outillage) : `robots.txt` inchange (`Allow: /`, aucun bot bloque, sitemap reference). `sitemap.xml` reconfirme toujours les 14 articles du pipeline en ligne sous `/articles/`, plus le second canal `/blog/` (`main-courante-electronique-vs-papier`) toujours present, inchange depuis le 2026-09-16.

## Alertes

- **Alerte haute — file de pull requests bloquee depuis 27 jours (34 jours sans nouvelle PR).** Les 14 PR d'articles produites par cet agent depuis le 2026-08-29 sont toujours en attente de relecture humaine (Phase 1, conformement aux instructions). Aucune fusion constatee sur GitHub. Signale avec une insistance croissante depuis plusieurs semaines sans reponse ; ce point franchit desormais le cap du mois complet.
- **Alerte — acces Search Console toujours refuse (403).** Deuxieme test technique reel consecutif sur `sc-domain:optimasprotect.ma` : permission insuffisante. Necessite une action humaine cote Google Search Console pour accorder l'acces au compte connecte.
- Alertes deja reportees, non reverifiees en detail ce run (voir `.seo/repo-map.md`) : mecanisme de publication reel du depot vers le site live malgre l'absence de fusion GitHub, second canal `/blog/` independant de ce depot, ecart de branding historique OptImasProtect/OptimasProtect sur l'habillage du site.

## Moyen technique utilise et note operationnelle

Connecteur MCP GitHub (`mcp__remote-devices__GitHub_MCP_Server__JS___*`) pour toutes les operations de lecture et d'ecriture sur le depot ce run. Bash fonctionnel localement en complement (encodage/decodage et verification d'integrite des fichiers `.seo/*`), mais son acces reseau externe est restreint par le proxy de la session (tunnel refuse hors liste blanche) : `api.github.com` repond mais reste bloque au niveau applicatif sur ce depot precis (jeton `GITHUB_TOKEN` present mais non habilite, `add_repo` requis).

**Note operationnelle : `.seo/backlog.md` n'a pas ete mis a jour ce run.** Le fichier a atteint une taille (~24 Ko decodes, ~32 Ko en base64) qui rend sa reproduction fiable par un aller-retour base64 manuel risquee dans cette session (une premiere tentative de mise a jour a produit un contenu corrompu, detecte avant toute ecriture sur le depot et donc sans consequence sur le fichier publie). Aucune donnee du depot n'a ete alteree. La totalite des observations de ce run est consignee ci-dessus ; la mise a jour de `.seo/backlog.md` est reportee a un prochain run, idealement via un outil de lecture/ecriture moins sujet a ce risque (par exemple un acces direct fonctionnel via `add_repo`, qui reglerait aussi l'alerte GITHUB_TOKEN documentee depuis les premiers runs).
