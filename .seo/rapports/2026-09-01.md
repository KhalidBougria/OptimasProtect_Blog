# Rapport de veille SEO et production éditoriale — 2026-09-01

## Bootstrap

- Accès au dépôt `KhalidBougria/OptimasProtect_Blog` : fonctionnel via le connecteur GitHub MCP local (`local_unpacked_qunfei-wu_github-mcp-server-js`), authentifié en tant que `KhalidBougria` (propriétaire du compte) et non `obougria` comme prévu au prompt de référence. Permissions confirmées `push: true`, `admin: true`. Dépôt visible comme `public` côté API (le prompt le décrit comme privé).
- `.seo/agent-prompt.md` existe déjà (version 3.2, dernière PR de correction #11 mergée le 2026-08-29) : relu pour les corrections de fond qu'il porte par rapport au texte de référence reproduit dans la tâche planifiée — voir alertes ci-dessous.
- `.seo/repo-map.md` et `.seo/backlog.md` existants et à jour au 2026-08-31 : repris comme base de continuité pour ce run plutôt que redémarrés à zéro.

## Alerte haute — divergences entre le texte de la tâche planifiée et la version de référence réellement en vigueur dans le dépôt

Le prompt de référence du dépôt (version 3.2) corrige trois points par rapport au texte reproduit dans la définition de cette tâche planifiée, et ces corrections ont été appliquées ce run :

1. **Base légale :** la loi applicable est la **Loi n°27-06** relative aux activités de gardiennage et de transport de fonds (dahir n°1-07-155 du 30 novembre 2007, Bulletin officiel n°5584 du 6 décembre 2007), et non « Loi n°35-09 » comme l'indique le texte de la tâche planifiée.
2. **Structure d'URL du blog :** `/articles/{slug}` (confirmé par le lien footer du site live), et non `/blog/{slug}`.
3. **Orthographe de la marque :** `OptimasProtect` en un seul mot (conforme au site live), et non « Optimas Protect » en deux mots.

Ces trois corrections ont été confirmées par l'utilisateur lors de runs précédents (2026-08-22 et 2026-08-29) et sont documentées dans `.seo/agent-prompt.md` et `.seo/repo-map.md`. **Recommandation : mettre à jour le texte de la tâche planifiée elle-même** pour refléter ces corrections, afin d'éviter que de futurs runs ne partent d'un texte obsolète avant de le corriger à nouveau via le dépôt.

## Alerte haute — toujours aucun générateur de site connecté

Le dépôt ne contient aucun générateur de site statique (`astro.config.*`, `next.config.*`, `hugo.toml`, `package.json`, etc.), seulement du contenu Markdown et le suivi `.seo/`. Aucun article produit par cet agent ne peut devenir une page live sur `optimasprotect.ma` tant qu'un humain n'a pas branché un générateur sur ce dépôt (ou relié le générateur existant du site à ce dépôt). Non résolu depuis les premiers runs.

## Alerte — volume de PR ouvertes en croissance, aucune fusion

Le dépôt compte désormais **12 pull requests ouvertes depuis le 2026-08-22, aucune fusionnée** — plus d'une semaine et demie sans relecture humaine. Ce n'est pas bloquant pour la veille ni la production (la Phase 1 fonctionne comme prévu), mais le volume croissant devient un point à réévaluer par l'utilisateur : au-delà d'un certain seuil, produire sans qu'aucune PR ne soit jamais relue perd de son intérêt.

## Veille mots-clés et SERP

Autocomplete (`gl=ma`, `hl=fr`) sur 8 requêtes ciblées pilier 2 et cluster 9, en complément de la veille déjà réalisée les jours précédents sur les clusters prioritaires. Marché structurellement pauvre en autocomplete, cohérent avec les runs précédents :

- « faux pointage agent de sécurité », « contrôler ses agents de sécurité à distance », « transparence prestations sécurité privée », « société de sécurité litige client prestation » : 0 suggestion.
- « portail client sécurité » : une seule suggestion, hors sujet (« espace client sécurité sociale »).
- « gestion des incidents sécurité » : suggestions dominées par la cybersécurité (ANSSI, ISO 27035), confirmant la mise en garde du prompt de référence sur les têtes de requête génériques à ne pas attaquer frontalement.
- « rapport d'incident agent de sécurité » : signal intéressant (« exemple rapport d'incident agent de sécurité pdf »), mais intention trop proche de la PR #10 déjà existante (rapport d'intervention ADS) — non retenu comme sujet distinct ce run pour éviter un doublon d'intention.

Google Trends (`geo=MA`, 12 mois) sur 3 mots-clés pilier 2 / cluster 9 : plat à 0 sauf deux pics isolés à un seul point chacun, cohérent avec le comportement déjà documenté sur ce marché (à interpréter avec prudence sur un volume quasi nul).

**Recherche `site:optimasprotect.ma`** : reconfirmée à 0 résultat indexé ce run.

## Veille concurrentielle

- **SEKUR Africa** : aucun changement structurel détecté depuis le 2026-08-31. Le site `sekur.fr` a publié un nouveau comparatif « meilleurs logiciels sécurité privée 2026 », succédant à la version 2025 déjà documentée — signal de fraîcheur éditoriale du concurrent à surveiller, sans changement de positionnement Maroc identifié. L'opacité tarifaire en dirhams sur le marché marocain reste confirmée côté SEKUR Africa.
- **Trackforce Valiant** : aucun changement détecté, toujours positionné à l'international, pricing sur devis.
- Aucun nouvel acteur marocain détecté.

## Scoring et sélection

Aucun sujet pilier 2 réellement neuf n'a passé le seuil de 14/25 ce run. Conformément à la règle de bascule (§0 du prompt de référence), la production est restée sur le pilier 1, avec le sujet le plus ancien de la file d'attente établie le 2026-08-31 :

| Sujet | Pilier | Score /25 | Décision |
|---|---|---|---|
| Faux pointage ADS : le détecter sans accuser | 1, croisé 1/2, cluster 9 | 16 | **Produit et déposé en PR #14** |
| Rondes électroniques obligatoires (conformité) | 1, cluster 7 | 15 | En file. Décision reportée : enrichir PR #8 vs nouvel article, à trancher après relecture complète de PR #8. |
| Conformité marchés publics sécurité | 1, cluster 7 | 15 | En file. Même décision en attente. |

## Production et publication

**1 article produit et déposé en PR** : PR #14, `faux-pointage-ads-detecter-maroc` (score 16/25, pilier 1, non réglementaire). Angle douleur → solution, distinct de PR #1 (« comment ça marche ») : signaux à vérifier avant d'accuser un agent, comparatif registre papier / preuve NFC, posture non accusatoire envers les agents conformément au garde-fou du prompt de référence. Vérifié anti-doublon contre les 11 branches `article/*` existantes. Prix (99 / 179 MAD HT par smartphone et par mois) et conversion journalière conformes. Liens internes vérifiés (hub pilier 1, hub prix). JSON-LD `SoftwareApplication` + `Offer` et `FAQPage` validés en JSON. Reste en Phase 1 (relecture par PR), branche `article/faux-pointage-ads-detecter-maroc`, PR ouverte vers `main`.

`.seo/backlog.md` et `.seo/repo-map.md` mis à jour en conséquence (commits directs sur `main`, ce sont des documents de suivi et non des articles).

## Compteur ratio hebdomadaire (semaine S36, cumul sur les PR ouvertes)

Répartition par pilier sur les 12 PR ouvertes : pilier 1 = 7/12 (≈58 %), pilier 2 = 3/12 (≈25 %), pilier 3 = 2/12 (≈17 %). Cible §0 : 60/30/10. Pilier 1 proche de la cible, pilier 2 légèrement sous la cible, pilier 3 légèrement au-dessus. Prochaine session : privilégier un sujet pilier 2 réellement neuf si le seuil de 14/25 est atteint en veille, sinon rester sur le pilier 1 conformément à la règle de bascule.

## Synthèse de fin de run

- **Moyen technique utilisé pour écrire sur GitHub :** connecteur MCP GitHub local (`local_unpacked_qunfei-wu_github-mcp-server-js`), et non l'API directe.
- **Accès au dépôt :** fonctionnel (authentifié comme `KhalidBougria`, propriétaire, et non `obougria` comme prévu — voir alerte ci-dessus).
- **Articles produits/publiés/mis à jour :** 1 article produit et déposé en PR (PR #14). Aucune mise à jour d'article existant. Aucune fusion (Phase 1, relecture humaine requise).
- **Alertes hautes :** (1) divergence entre le texte de la tâche planifiée et la version de référence réellement en vigueur dans le dépôt sur trois points (base légale, structure d'URL, orthographe de la marque) — recommandation de mettre à jour le texte de la tâche ; (2) absence de générateur de site connecté au dépôt, toujours non résolue ; (3) 12 PR ouvertes sans aucune fusion depuis plus d'une semaine et demie, à réévaluer par l'utilisateur.
