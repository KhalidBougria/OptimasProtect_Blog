# Rapport de veille SEO & production éditoriale — 2026-09-11

## Résumé

1 article produit et publié en PR (draft, phase 1) : **Contrôle qualité d'une prestation de gardiennage au Maroc** (pilier 2, score 17/25). Priorité donnée au pilier 2 pour corriger l'écart de ratio signalé sans interruption depuis le 2026-08-31. Veille menée jusqu'au bout (mots-clés, SERP, concurrence). Une alerte outillage (bac à sable Bash indisponible) a été contournée avec succès via le navigateur intégré, sans impact sur la qualité du travail.

## Bootstrap

- Accès au dépôt `KhalidBougria/OptimasProtect_Blog` confirmé : authentifié en tant que `KhalidBougria` (propriétaire), permissions `push: true` / `admin: true`. Voie technique : connecteur MCP GitHub local (`local_unpacked_qunfei-wu_github-mcp-server-js`).
- `.seo/agent-prompt.md` déjà présent sur `main` (version 3.2, incluant les corrections du 2026-08-22 sur la Loi n°27-06 et du 2026-08-29 sur l'URL `/articles/{slug}` et l'orthographe `OptimasProtect` en un mot). Lu en entier, utilisé comme référence pour ce run.
- `.seo/repo-map.md` et `.seo/backlog.md` déjà présents, lus en entier et mis à jour ce run.
- Search Console (`sc-domain:optimasprotect.ma`) : accès en lecture toujours non configuré pour cette tâche planifiée, conformément au bootstrap. Veille appuyée sur l'autocomplete et les positions SERP observées.

## Veille mots-clés et SERP

Rotation d'environ 25 requêtes (`gl=ma`, `hl=fr`), priorité pilier 2 : notation prestation sécurité privée, avis client société de sécurité Maroc, signalement incident gardiennage, application client société de sécurité, gestion réclamation client sécurité privée, assurance qualité gardiennage, bouton alerte panique agent de sécurité, rapport de ronde en ligne client, suivi qualité prestation gardiennage, rapport de gardiennage mensuel client, preuve de service société de sécurité, contester facture société de gardiennage, indicateur de performance société de sécurité, justificatif de ronde sécurité, document ronde de sécurité, preuve ronde de sécurité client, comment prouver qu'une ronde a été effectuée, litige client agent de sécurité preuve, prestation de gardiennage non effectuée que faire ; pilier 1 : logiciel de gardiennage Casablanca, badge de présence agent de sécurité, audit rondes de sécurité, solution de ronde connectée, preuve de passage ronde sécurité, contrôle prestation gardiennage client, litige prestation gardiennage ; pilier 3 : SIRH agent de sécurité Maroc, badge pointage agent de sécurité.

**Un seul signal exploitable** : `assurance qualité gardiennage` → suggestion `contrôle qualité sécurité privée`. Régime pauvre en autocomplete confirmé une nouvelle fois sur ce marché (cohérent avec les runs précédents).

`trends_interest` (`geo=MA`, 12 mois) sur `justificatif de ronde`, `preuve de passage`, `document ronde de sécurité` : deux pics isolés sur des semaines distinctes, aucune série exploitable.

## Scoring

| Sujet | Pilier | Intention (5) | Rank (5) | Local (5) | Volume (5) | Fraîcheur (5) | Total |
|---|---|---|---|---|---|---|---|
| Contrôle qualité d'une prestation de gardiennage | 2 | 4 | 4 | 3 | 2 | 4 | **17/25** |

Seuil de production (14/25) atteint. Intention vérifiée distincte des 13 articles déjà en PR, en particulier `portail-client-securite-privee-maroc` (ce que le portail donne à voir) et `rapport-intervention-ads-maroc` (format du document d'incident) — aucun des deux ne traite la méthodologie de contrôle qualité elle-même.

## Veille concurrentielle

- SEKUR / SEKUR Africa : aucun changement de positionnement ou de tarification détecté par rapport aux runs précédents (EUR uniquement pour l'offre France, opacité MAD toujours confirmée côté Maroc).
- Trackforce Valiant : positionnement international inchangé, pricing sur devis.
- Aucun nouvel acteur marocain détecté.
- Recherche web sur l'angle contrôle qualité : un article de presse (Le Matin.ma) mentionne une intensification des contrôles de l'inspection du travail en 2024 sur les entreprises de sécurité (1 022 visites, 9 160 observations). **Source non autorisée au sens du §8.3** (presse, pas une source primaire de la liste officielle) : ce chiffre n'a pas été repris dans l'article produit.
- `site:optimasprotect.ma` : 0 résultat pertinent indexé (recherche web), seuls des homonymes sans rapport remontent.

## Production

**PR #16 — Contrôle qualité d'une prestation de gardiennage au Maroc**
- Slug : `controle-qualite-prestation-gardiennage-maroc`
- Pilier 2, mot-clé focus « contrôle qualité sécurité privée », score 17/25
- Contenu : intention répondue dans les 100 premiers mots, tableau comparatif méthodes traditionnelles vs traçabilité numérique, exemple concret chiffré, liste d'indicateurs exploitables, section honnête « à qui ce modèle ne convient pas », FAQ de 5 questions (FAQPage), JSON-LD SoftwareApplication + Offer (99 / 179 MAD HT), 3 liens internes vérifiés contre des fichiers réels sur leurs branches `article/*`
- Reste en Phase 1 (branche `article/controle-qualite-prestation-gardiennage-maroc`, PR draft, relecture humaine requise avant merge)
- Lien : https://github.com/KhalidBougria/OptimasProtect_Blog/pull/16

## Ratio hebdomadaire (§0) — 14 PR ouvertes après ce run

| Pilier | PR | Part |
|---|---|---|
| 1 (traçabilité / preuve) | 8 | ~57 % |
| 2 (incidents / portail client) | 4 | ~29 % |
| 3 (RH / opérationnel) | 2 | ~14 % |

Cible 60/30/10. Le pilier 2 se rapproche de sa cible grâce à l'ajout de ce run. Prochaine session : continuer à privilégier le pilier 2 si un sujet réellement neuf passe le seuil de 14/25, sinon revenir au pilier 1 conformément à la règle de bascule.

## Alertes

**Haute — aucun générateur de site connecté au dépôt.** Constaté lors des runs précédents (dernière vérification structurelle le 2026-09-09) : la racine de `main` ne contient que le dossier `.seo/`, aucun générateur de site statique, aucun `.github/workflows`. Sans ce branchement, aucun article produit par cet agent ne peut devenir une page live sur `optimasprotect.ma`. Non revérifié en profondeur ce run faute d'accès Bash, mais rien n'indique de changement. **Toujours non résolu.**

**Haute, croissante — 14 pull requests ouvertes depuis le 2026-08-22, aucune fusionnée, soit 20 jours sans relecture humaine.** Ce point est signalé sans interruption depuis le 2026-08-31 et continue de croître en ancienneté. Non bloquant pour la veille et la production (la Phase 1 fonctionne comme prévu), mais le volume croissant de PR en attente sans aucune relecture mérite une réévaluation.

**Moyenne, résolue ce run — bac à sable Bash indisponible.** Le shell de cette session est resté inaccessible toute la durée du run (erreur de montage liée à une mise à jour Windows du 8 septembre, déjà rencontrée lors du run du 2026-09-10, panne connue de la plateforme). Contournement appliqué avec succès via le navigateur intégré : lecture des fichiers `.seo/*.md` en texte brut via `raw.githubusercontent.com`, encodage base64 des contenus à committer via `TextEncoder`/`btoa` exécuté côté page avant chaque appel `create_or_update_file`. Aucune perte de qualité constatée ce run, mais le contournement multiplie les appels ; à surveiller si le problème persiste sur plusieurs runs.

## Moyen technique d'écriture GitHub

Connecteur MCP GitHub local (`local_unpacked_qunfei-wu_github-mcp-server-js`), authentifié sur le compte propriétaire `KhalidBougria`. Accès au dépôt fonctionnel (push confirmé par les 3 commits de ce run : article, backlog, repo-map, plus l'ouverture de la PR #16).
