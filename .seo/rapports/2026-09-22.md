# Rapport quotidien - 2026-09-22

## Résumé

Écart de run de 4 jours : le dernier rapport quotidien disponible était daté du 2026-09-18 (aucun rapport pour le 09-19, 20, 21, dont un lundi le 09-21 qui aurait dû porter une synthèse hebdomadaire — la tâche planifiée ne semble pas avoir tourné ces jours-là, hors du contrôle de cet agent). État du dépôt et du site inchangé sur le fond. Aucun nouvel article produit ni mis à jour. Deux nouveaux logiciels concurrents internationaux identifiés (BanetteOne, eBrigade) et écartés du cercle Maroc après vérification (France uniquement).

## Moyen technique utilisé pour écrire sur GitHub

Connecteur MCP GitHub local (`mcp__GitHub_MCP_Server__JS___*`), authentifié en tant que KhalidBougria (propriétaire du compte), permissions push:true / admin:true confirmées. L'accès au dépôt a fonctionné normalement pour toutes les opérations de lecture et d'écriture de ce run.

Alerte outillage nouvelle ce run : le bac à sable Linux (Bash) de cette session a échoué au démarrage avec un message différent de l'erreur de montage Windows documentée sans interruption depuis le 8 septembre (« Workspace unavailable. The isolated Linux environment failed to start (VM service not running. Restart your computer to restore it.) »). Bash est resté indisponible toute la durée du run. Contournement appliqué avec succès via le navigateur intégré (fetch + atob/btoa via javascript_tool) combiné à l'API GitHub MCP pour l'ensemble des opérations, y compris une correction en cours de run : un premier commit de mise à jour de `.seo/repo-map.md` a produit un fichier tronqué (10500 octets au lieu des ~41 Ko attendus) suite à une erreur de reconstruction du contenu encodé en base64 par chunks ; l'erreur a été détectée via la taille de fichier anormale retournée par l'API, puis corrigée par un second commit reconstruisant le contenu complet (historique intégral + nouvelle section) directement depuis le blob Git d'origine, avec vérification a posteriori (présence de tous les marqueurs de section, tête et queue du fichier conformes).

## Repository access

Accès confirmé et fonctionnel (repository KhalidBougria/OptimasProtect_Blog, public côté API). 14 branches article/*, 14 PR ouvertes, aucun changement d'état depuis le dernier rapport.

## Articles produits / publiés / mis à jour

0 nouvel article produit. 0 mise à jour d'article. Aucun sujet n'a franchi le seuil de production de 14/25 ce run (voir section Veille mots-clés ci-dessous). Répartition par pilier inchangée depuis le 2026-09-11 : pilier 1 = 8/14 (~57%), pilier 2 = 4/14 (~29%), pilier 3 = 2/14 (~14%), proche de la cible 60/30/10 (règle de bascule non déclenchée, aucun sujet pilier 2/3 n'ayant émergé de toute façon).

## Veille mots-clés et SERP

Marché ciblé : Maroc (gl=ma, hl=fr). 13 requêtes testées sur des angles non essayés précédemment : avis client société de gardiennage Maroc, comparatif logiciel gardiennage, société de sécurité amende cahier des charges, modèle main courante gardiennage, erreur de pointage agent de sécurité, loi 32.26 agents de sécurité, gestion de flotte agents de sécurité, rapport de ronde PDF, société de sécurité cahier des charges type, combien coûte une ronde de sécurité, journée de travail agent de sécurité Maroc loi, logiciel de sécurité privée avis, cahier des charges sécurité privée modèle.

Deux suggestions autocomplete obtenues sur 13, toutes deux déjà couvertes par des articles existants (vérifié par lecture intégrale) :
- « modèle main courante gardiennage » → « modèle main courante sécurité pdf » / « modèle main courante sécurité excel » — déjà couvert par main-courante-electronique-maroc (PR4).
- « rapport de ronde PDF » → « rapport de ronde pdf » / « rapport ronde de sécurité pdf » — déjà couvert par modele-rapport-de-ronde-maroc (PR7).

trends_interest (geo=MA, 12 mois) sur ces deux intentions : intérêt à 0 sur toutes les régions marocaines disponibles, aucune requête associée. Régime pauvre en signal de demande mesurable confirmé, conforme au constat répété depuis plusieurs semaines.

## Veille concurrentielle

SEKUR Africa, EasyGard/SGGI, Trackforce Valiant : non revérifiés en détail ce run (dernière vérification du 09-16/09-18 jugée suffisamment récente), aucun changement attendu.

Recherche ciblée sur de nouveaux entrants : « TRACKTIK » identifié dans les résultats mais il s'agit du même éditeur que Trackforce Valiant (fusion ancienne), pas un nouvel acteur. Deux logiciels non documentés jusqu'ici vérifiés directement par navigation :
- **BanetteOne** (banetteone.com) : liste de clients aux noms français (National France Sécurité, Opale Picarde Sécurité), aucune mention du Maroc.
- **eBrigade** (ebrigade.app) : cite explicitement la carte professionnelle CNAPS, les qualifications SSIAP et la convention collective Prévention Sécurité — cadre réglementaire strictement français.

Conclusion : les deux solutions sont France uniquement, traitées comme cercle 2 (international, hors cible géographique), non ajoutées au cercle 1 Maroc. Documentées dans repo-map.md pour éviter une revérification inutile.

## Alertes

**Haute — nouvelle ce run :** échec du bac à sable Bash avec un message d'erreur différent de celui documenté depuis le 8 septembre (voir section outillage ci-dessus). Cause potentiellement distincte de l'erreur de montage Windows habituelle, à surveiller sur les prochains runs.

**Haute — nouvelle ce run, résolue en cours de run :** commit tronqué de `.seo/repo-map.md` (10500 octets au lieu de ~41 Ko) causé par une erreur de reconstruction du contenu base64 concaténé par chunks dans le contournement navigateur. Détectée et corrigée dans le même run (second commit avec contenu complet reconstruit et vérifié).

**Portées (carried forward, non résolues, hors périmètre d'action de cet agent) :**
1. Mécanisme de publication réel du dépôt vers le site live toujours non identifié avec précision (le contenu atteint le site sans fusion de PR).
2. Second canal `/blog/` indépendant de ce dépôt publiant du contenu sur optimasprotect.ma avec risque de cannibalisation SEO sur le cluster main courante électronique.
3. File de 14 PR d'articles ouvertes depuis le 2026-08-22, soit 31 jours au 2026-09-22, aucune fusionnée sur GitHub.
4. Écart de branding : le site live affiche « OptImasProtect » (I majuscule médian) dans son habillage, alors que le contenu éditorial utilise « OptimasProtect » en un mot.
5. site:optimasprotect.ma non retesté ce run (outil de recherche web documenté comme limité aux résultats US, fiabilité incertaine pour Google.ma sans Search Console).

## Décision de production

Aucun sujet n'a franchi le seuil de 14/25 ce run. Conforme à la règle qu'une journée sans article publié est normale, une journée sans veille ne l'est pas (paragraphe 4.1), et à la priorité qualité avant quota (paragraphe 8.4). Aucune synthèse hebdomadaire requise aujourd'hui (mardi, pas lundi).
