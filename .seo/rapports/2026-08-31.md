# Rapport de veille SEO & production éditoriale — OptimasProtect
## 2026-08-31

Premier run de la semaine S36 (lundi). Semaine précédente S35 (24—30 août) : 6 articles produits contre une cible de 2 à 3/semaine (largement dépassée).

## Bootstrap

- Accès au dépôt `KhalidBougria/OptimasProtect_Blog` confirmé via le connecteur GitHub MCP local (`local_unpacked_qunfei-wu_github-mcp-server-js`), authentifié en tant que `KhalidBougria` (propriétaire du compte), pas `obougria` comme prévu au prompt de référence — cohérent avec tous les runs précédents. Permissions confirmées : `push: true`, `admin: true`.
- `.seo/agent-prompt.md` déjà présent sur `main` (version 3.2), lu en entier. Aucune divergence détectée par rapport au run précédent. Rappel : cette version corrige deux points par rapport au texte reproduit dans la tâche planifiée elle-même — la loi applicable est la **Loi n°27-06** (et non « Loi n°35-09 »), et la structure d'URL cible est **`/articles/{slug}`** (et non `/blog/{slug}`) ; la marque s'écrit désormais **`OptimasProtect`** en un seul mot pour la production future.
- `.seo/repo-map.md` et `.seo/backlog.md` déjà présents, lus en entier avant toute action, puis mis à jour ce run (commits directs sur `main`, fichiers de suivi hors contenu éditorial).
- Search Console : toujours non configurée pour cette tâche planifiée (conforme au bootstrap). Veille appuyée sur l'autocomplete Google (`gl=ma`, `hl=fr`) et Google Trends (`geo=MA`).

## Veille mots-clés & SERP

Autocomplete sur 15 requêtes en rotation, couvrant le cluster 8 prioritaire (obligations Loi n°27-06) et les deux nouveaux candidats du backlog, plus une rotation générale pilier 1/2/3 :

| Requête | Résultat |
|---|---|
| obligations loi 27-06 employeur | 0 suggestion |
| rondes électroniques obligatoires | 0 suggestion |
| conformité marchés publics sécurité | 0 suggestion |
| loi 27-06 sécurité privée | 1 suggestion : « loi 27 06 relative à la sécurité privée au maroc pdf » |
| portail client sécurité privée | 0 suggestion |
| logiciel gestion incidents sécurité | 0 suggestion |
| CNSS agent de sécurité | 0 suggestion |
| agrément société de sécurité maroc | 0 suggestion |
| main courante électronique gratuite | 2 suggestions (dont « télécharger main courante électronique gratuite ») |
| controle de ronde prix maroc | 1 suggestion : « contrôleur de ronde prix maroc » (variante pouvant évoquer un équipement physique, à surveiller) |
| planning agents de sécurité gratuit excel | 1 suggestion (variante orthographique) |
| faux pointage agent de sécurité | 0 suggestion |
| journée de travail agent de sécurité maroc | 0 suggestion |
| logiciel gardiennage Maroc (mode étendu) | 0 suggestion |

Trends (`geo=MA`, 12 mois) sur « obligations loi 27-06 », « rondes électroniques obligatoires », « cahier des charges gardiennage » : plat à 0 partout, sauf un pic isolé à 100 pour « rondes électroniques obligatoires » le 2026-01-18 (un seul point sur 52, à interpréter avec prudence sur un volume quasi nul). Cohérent avec le constat récurrent des runs précédents : le marché francophone marocain du logiciel de gardiennage reste structurellement pauvre en signal organique classique ; l'autocomplete et les volumes rapportés par l'utilisateur restent les boussoles les plus fiables sur ce marché jeune.

## Veille concurrentielle

- **SEKUR Africa** : la page produit dédiée Maroc ne publie toujours aucun prix inline. Nouveau signal : le site français `sekur.fr` (hors périmètre Maroc) affiche désormais un prix d'appel distinct à partir de 69,99 € HT/mois, en plus de la grille `/tarifs/` déjà documentée (59,99 à 29,99 € HT/mois selon engagement, 10 licences). Aucune déclinaison en dirhams détectée sur aucune des deux pages. L'opacité tarifaire marocaine reste confirmée et reste un point d'appui éditorial pour OptimasProtect.
- **Trackforce Valiant** : toujours positionné à l'international (300 000+ professionnels, 30 000+ sites, 45 pays), pricing toujours sur devis, aucune adaptation Maroc identifiée ce run.
- Aucun nouvel acteur marocain détecté.
- `site:optimasprotect.ma` : reconfirmé à 0 résultat indexé (recherche web).

## Anti-doublon

10 branches `article/*` existantes avant ce run, toutes en PR ouverte (aucune fusionnée). Vérification de l'intention du sujet retenu (« obligations de l'employeur au titre de la Loi n°27-06 ») contre les 10 articles existants : aucun ne la couvre. Distinct de `cahier-des-charges-gardiennage-maroc` (exigences d'un marché/CPS) et de `loi-32-26-agents-securite-maroc` (durée du travail, 8h/jour) — confirmé par lecture intégrale de ces deux fichiers avant rédaction.

## Scoring des sujets

| Sujet | Pilier | Score /25 | Décision |
|---|---|---|---|
| Obligations de la Loi n°27-06 pour l'employeur | 1, cluster 8, hub conformité | **18** | Produit (PR #13) — en tête de file depuis le 2026-08-28, reporté trois fois pour cadence/incertitude technique, désormais résolues |
| Rondes électroniques obligatoires (conformité) | 1, cluster 7 | **15** | En file. Décision enrichissement PR #8 vs article dédié reportée à la prochaine session |
| Conformité marchés publics sécurité | 1, cluster 7 | **15** | En file. Même décision en attente |
| Faux pointage ADS : détecter sans accuser | 1/2 croisé, cluster 9 | **16** | En file depuis le 2026-08-26, inchangé, prêt à produire |

## Production

**1 article produit et déposé en pull request (Phase 1, `draft: true`) :**

- **`obligations-loi-27-06-employeur-maroc`** — pilier 1, cluster 8, hub conformité, score 18/25 — [PR #13](https://github.com/KhalidBougria/OptimasProtect_Blog/pull/13) (brouillon).
  - Angle : obligations propres à l'employeur au titre de la Loi n°27-06 elle-même (autorisation administrative, registre du personnel, embauche encadrée) plus obligations CNSS générales, distinctes des exigences d'un cahier des charges et de la durée du travail (loi n°32.26).
  - Article réglementaire (§3.3) : reste en PR en permanence, auteur `Team Optimas` / JSON-LD `Organization`, aucun avis juridique formulé, renvoi explicite vers un professionnel du droit ou l'autorité de tutelle.
  - Sources primaires citées et datées : dahir n°1-07-155 (30/11/2007, BO n°5584), décret n°2-09-97 (25/10/2010), CNSS (obligations générales), dahir/BO de la loi n°32.26 pour la distinction. Un point `[À VÉRIFIER]` assumé : lien direct vers le PDF exact du BO n°5584 non confirmé par recherche web ce run — cohérent avec le traitement déjà appliqué à `cahier-des-charges-gardiennage-maroc`.
  - 2 liens internes vérifiés anti-404 contre des fichiers réels du dépôt (`cahier-des-charges-gardiennage-maroc`, `loi-32-26-agents-securite-maroc`), en `/articles/{slug}`.
  - Frontmatter complet avec `canonical` (convention en vigueur depuis PR #12/#13).
  - Marque `OptimasProtect` en un mot, conforme à la version 3.2 du prompt de référence.

Aucune mise à jour d'article existant n'a été nécessaire (aucun doublon détecté).

## Ratio hebdomadaire (§0 — cible 60/30/10)

Cumul sur les 11 PR ouvertes à ce jour : pilier 1 = 6/11 (≈55 %), pilier 2 = 3/11 (≈27 %), pilier 3 = 2/11 (≈18 %). Proche de la cible, légèrement sous-représenté sur le pilier 2. Aucune bascule nécessaire ce run (le sujet pilier 1 produit était déjà en tête de file, prioritaire depuis plusieurs jours). Recommandation pour la suite de la semaine S36 : privilégier un sujet pilier 2 réellement neuf si un score ≥14/25 est atteint en veille, sinon poursuivre sur le pilier 1 conformément à la règle de bascule.

## État de publication

Aucun changement : les 11 PR d'articles (#1, #2, #4 à #10, #12, #13) restent **ouvertes, non fusionnées**, en Phase 1 conformément à la consigne de la tâche planifiée. Aucun commit direct sur `main` en dehors des fichiers `.seo/` (backlog, repo-map, ce rapport).

## Alertes

### 🔴 Alerte haute n°1, toujours active — absence de générateur de site connecté au dépôt

Aucun générateur de site statique n'est présent dans `KhalidBougria/OptimasProtect_Blog` (aucun `astro.config.*`, `next.config.*`, `hugo.toml`, `package.json`, pipeline de build). Le site public `optimasprotect.ma` n'est ni indexé sur Google (`site:optimasprotect.ma` reconfirmé à 0 résultat), ni prouvé connecté à ce dépôt. **Aucun article produit par cet agent ne peut devenir une page live tant qu'un humain n'a pas branché un générateur de site sur ce dépôt (ou relié le générateur existant du site à ce dépôt).** Alerte ouverte depuis le premier run (2026-08-22), non résolue à ce jour — plus d'une semaine.

### 🟠 Alerte modérée (nouvelle formulation, situation déjà connue) — volume croissant de PR non relues

11 pull requests d'articles ouvertes depuis le 2026-08-22, **aucune fusionnée à ce jour** — plus d'une semaine sans relecture humaine. Ce n'est pas un dysfonctionnement de l'agent (la Phase 1 fonctionne comme prévu, aucune écriture directe sur `main` en dehors des fichiers `.seo/`), mais ce volume mérite d'être signalé explicitement : au-delà d'un certain nombre de PR en attente, continuer à produire de nouveaux articles sans qu'aucune relecture n'ait eu lieu devient une question d'arbitrage pour l'utilisateur plutôt qu'un choix technique de l'agent.

### Points non résolus, inchangés

- `robots.txt` : toujours non vérifiable (URL hors du périmètre de provenance autorisé pour l'outil de recherche web de cette session).

## Résumé technique de fin de run

- **Moyen technique d'écriture GitHub :** connecteur MCP GitHub local (`local_unpacked_qunfei-wu_github-mcp-server-js`), aucun appel direct à l'API GitHub hors MCP.
- **Accès au dépôt :** fonctionnel, écriture confirmée (branche créée, fichier commité, PR ouverte, 2 fichiers `.seo/` mis à jour).
- **Articles produits / publiés (mergés) / mis à jour ce run :** 1 produit (PR #13, brouillon) / 0 publié (aucun mécanisme de mise en ligne disponible — voir alerte haute) / 0 mis à jour.
- **Alertes hautes :** 1 active (absence de générateur de site connecté), inchangée. 1 alerte modérée signalée explicitement (volume de PR non relues).
