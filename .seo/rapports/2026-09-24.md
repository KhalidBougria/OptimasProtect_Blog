# Rapport quotidien — 2026-09-24

## Bootstrap

Accès au dépôt `KhalidBougria/OptimasProtect_Blog` vérifié et fonctionnel via le connecteur GitHub MCP (`mcp__remote-devices__GitHub_MCP_Server__JS___*`), authentifié en tant que **KhalidBougria** (propriétaire du compte, pas `obougria` comme prévu au prompt de référence — écart déjà documenté depuis les premiers runs). Permissions confirmées : `push: true`, `admin: true`. Visibilité du dépôt côté API : **public** (le prompt de référence et son tableau §7 indiquent « privé » — écart déjà connu, non actionnable par cet agent).

Le bac à sable Bash de cette session, documenté comme indisponible ou instable en continu depuis le 8 septembre dans les rapports précédents, est **fonctionnel de bout en bout ce run**. Un `GITHUB_TOKEN` et un `GH_TOKEN` sont présents dans l'environnement ; un test direct montre qu'ils authentifient bien l'utilisateur GitHub, mais un appel API direct sur ce dépôt précis est refusé (« GitHub access to this repository is not enabled for this session »). Le connecteur MCP GitHub reste donc la voie technique utilisée pour toutes les écritures de ce run, conformément au bootstrap.

`.seo/agent-prompt.md` (version 3.2, 264 lignes) relu intégralement : à jour, aucune divergence avec les corrections déjà actées (Loi n°27-06, URLs `/articles/{slug}`, marque **OptimasProtect** en un seul mot). Ces corrections priment sur le texte reproduit dans le corps de la tâche planifiée elle-même, qui diverge encore sur ces trois points — conformément aux instructions de bootstrap, la version du dépôt fait foi et a été appliquée intégralement ce run.

`.seo/repo-map.md` (cartographie du dépôt) relu intégralement (248 lignes avant mise à jour de ce run) : aucune réécriture de cartographie nécessaire, seulement l'ajout du point du jour.

## Veille mots-clés et SERP

Marché ciblé : Maroc (`gl=ma`, `hl=fr`). 16 requêtes testées ce run, sur des angles non essayés lors des runs précédents (voir liste complète dans `.seo/backlog.md`) : logiciel de pointage agent de sécurité sans smartphone, badge NFC agent de sécurité prix, comment digitaliser une ronde de sécurité, preuve de passage agent sécurité client, solution de pointage pour agents de sécurité Maroc, rapport de ronde en temps réel, application de ronde de sécurité Maroc, logiciel de gestion des réclamations sécurité privée, suivi des interventions de sécurité en ligne, logiciel de main courante numérique gardiennage, portail client gardiennage Maroc, logiciel de gestion des agents de sécurité Maroc, gestion des contrats agents de sécurité, comparateur logiciel sécurité privée, application ronde de sécurité gratuite, société de sécurité digitalisation Maroc.

**0 suggestion sur 16** — le résultat le plus faible de toute la série documentée (les runs précédents obtenaient généralement 1 à 2 suggestions par lot de 13 à 18 requêtes). Régime pauvre en autocomplete confirmé avec une intensité record. Aucun sujet réellement neuf (pilier 1, 2 ou 3) n'a franchi le seuil de production de 14/25 ce run ; aucun `trends_interest` ni `topic_demand_score` n'a été lancé faute de tout signal de départ à qualifier.

### Search Console

Première tentative technique réelle d'appel à `mcp__SEO_Advena__gsc_query` sur `sc-domain:optimasprotect.ma` (`country=mar`, 2026-08-25 à 2026-09-21). Résultat : **erreur 403**, « User does not have sufficient permission for site 'sc-domain:optimasprotect.ma' ». Ceci confirme techniquement, et non plus seulement par hypothèse au bootstrap, que le compte connecté à l'outil SEO_Advena n'a pas les droits sur cette propriété GSC. Aucune action possible côté agent : nécessite qu'un accès en lecture soit accordé côté Google Search Console. Conformément au bootstrap, la veille continue de s'appuyer sur les positions SERP observées et l'autocomplete en attendant.

## Veille concurrentielle

SEKUR Africa, EasyGard/SGGI, Trackforce Valiant : non revérifiés en détail ce run (dernières vérifications du 16 au 22 septembre jugées suffisamment récentes), aucun changement attendu.

Recherche web ciblée sur de nouveaux entrants marocains : découverte et vérification directe (fetch de la page produit) d'**IBE MAROC** (International Business Engineering, Casablanca, `ibe.ma`), éditeur du système **Guard Online**. Il s'agit d'un contrôleur de rondes **matériel** : boîtiers propriétaires (WM-5000P5+ sans GPS, WM5000L5 avec GPS) qui transfèrent les pointages par GPRS vers un serveur consultable en ligne. Aucun prix public affiché. Ce n'est pas un logiciel SaaS NFC/smartphone comparable à OptimasProtect mais un système de pointeuses/badgeuses physiques avec remontée réseau : classé **cercle 3** (substitut matériel, non surveillable comme concurrent logiciel direct), au même titre que KVER/SecuMall déjà documentés. Non ajouté au cercle 1 Maroc.

## Décision de production

**Aucun sujet n'a franchi le seuil de 14/25 ce run** (0 suggestion exploitable sur 16 requêtes, le résultat le plus faible de la série). **Aucun nouvel article produit ni mis à jour.** Conforme à la règle qu'une journée sans article publié est normale, une journée sans veille ne l'est pas (§4.1), et à la priorité qualité avant quota (§8.4). Les 10 clusters prioritaires du §4.2 restent tous couverts par au moins un article en PR.

Répartition par pilier inchangée depuis le 2026-09-11 : pilier 1 = 8/14 (~57 %), pilier 2 = 4/14 (~29 %), pilier 3 = 2/14 (~14 %), proche de la cible 60/30/10.

## État du dépôt et file de pull requests

14 branches `article/*` et 14 PR ouvertes reconfirmées (numéros 1, 2, 4, 5, 6, 7, 8, 9, 10, 12, 13, 14, 15, 16) : **aucune fusion depuis le 2026-08-29** (première PR d'article), soit **26 jours** au 2026-09-24 ; aucune nouvelle PR depuis le 2026-08-22, soit 33 jours. Seules les PR 3 et 11 (corrections du prompt de référence) sont fusionnées.

## Alertes

- **Alerte haute — file de pull requests bloquée depuis 26 jours (33 jours sans nouvelle PR).** Les 14 PR d'articles produites par cet agent depuis le 2026-08-29 sont toujours en attente de relecture humaine (Phase 1, conformément aux instructions). Aucune fusion constatée sur GitHub. Signalé avec une insistance croissante depuis plusieurs semaines sans réponse ; ce point franchit désormais le cap du mois complet.
- **Alerte — accès Search Console confirmé refusé (403).** Premier test technique réel effectué ce run sur `sc-domain:optimasprotect.ma` : permission insuffisante. Nécessite une action humaine côté Google Search Console pour accorder l'accès au compte connecté.
- Alertes reportées sans nouvelle vérification ce run (voir `.seo/repo-map.md` pour le détail) : mécanisme de publication réel du dépôt vers le site live, second canal `/blog/` indépendant de ce dépôt, écart de branding OptImasProtect/OptimasProtect sur l'habillage du site.

## Moyen technique utilisé

Connecteur MCP GitHub (`mcp__remote-devices__GitHub_MCP_Server__JS___*`) pour toutes les opérations de lecture et d'écriture sur le dépôt. Bash (fonctionnel ce run) utilisé en complément pour l'encodage/décodage local des fichiers `.seo/*`. Un jeton `GITHUB_TOKEN` présent dans l'environnement Bash s'authentifie auprès de l'API GitHub mais n'a pas accès à ce dépôt précis dans cette session (nécessiterait un `add_repo` explicite) ; il n'a donc pas été utilisé comme voie d'écriture.
