# Rapport de veille SEO & production éditoriale — Optimas Protect

**Date :** 2026-08-30 (dimanche, semaine ISO 2026-W35, dernier jour de la semaine)

## Bootstrap

- **Accès dépôt :** confirmé via le connecteur MCP GitHub local (`local_unpacked_qunfei-wu_github-mcp-server-js`), authentifié en tant que `KhalidBougria` (propriétaire du compte, pas `obougria`). Permissions `push: true`, `admin: true`. Écriture directe sur `main` et sur les branches `.seo/*` opérationnelle (utilisée pour ce rapport et les mises à jour de cartographie ci-dessous).
- **Prompt de référence :** `.seo/agent-prompt.md` existait déjà, version 3.2, lu en entier. Il prime sur le texte reproduit dans la tâche planifiée. Deux corrections notables par rapport à la version reproduite dans la tâche planifiée elle-même :
  - la loi applicable est la **Loi n°27-06** (gardiennage et transport de fonds, dahir n°1-07-155, 2007), et non « Loi n°35-09 » ;
  - la structure d'URL cible est **`/articles/{slug}`**, et non `/blog/{slug}` ;
  - la marque s'écrit désormais **`OptimasProtect`** en un seul mot pour la production future (et non « Optimas Protect » en deux mots).
- **Search Console :** toujours non configurée pour cette tâche planifiée, conformément au bootstrap. Veille appuyée sur autocomplete et Google Trends comme prévu.

## Résolution d'une alerte critique héritée du run précédent (2026-08-29)

Le run du 2026-08-29 avait suspendu toute nouvelle production à cause d'un doute sur la structure d'URL (`/articles` découverte sur le site live vs `/blog/` utilisée dans les 9 PR déjà ouvertes) et d'un écart de marque non arbitré. Entre ce run et aujourd'hui, l'utilisateur a arbitré les deux points via la PR #11 (`fix/articles-path-and-brand-correction`, mergée le 2026-08-29 à 08:25:27Z), qui met à jour `.seo/agent-prompt.md` à la version 3.2. J'ai vérifié par un spot-check sur l'article de la PR #1 (`controle-de-ronde-nfc-gardiennage-maroc`) que son contenu utilise déjà des liens internes en `/articles/{slug}` et la forme `OptimasProtect` (un mot) dans le corps et le JSON-LD — les 9 articles en PR semblent donc déjà conformes aux deux points, malgré une description de PR #11 qui laissait entendre que seule la structure d'URL avait été corrigée rétroactivement, pas la marque. Point à confirmer plus finement (relecture complète des 9 fichiers) avant toute fusion, mais rien ne bloque à ce stade.

J'ai mis à jour `.seo/repo-map.md` et `.seo/backlog.md` (commits directs sur `main`) pour refléter cette résolution et lever la pause liée à cette incertitude.

## Alerte haute toujours active

**Aucun générateur de site n'est connecté à ce dépôt** (aucun `astro.config.*`, `next.config.*`, `hugo.toml`, `package.json`, pipeline de build). Tant qu'un humain n'a pas branché un générateur de site statique sur ce dépôt (ou relié le générateur existant d'`optimasprotect.ma`), aucun article produit par cet agent ne peut devenir une page live, quelle que soit la structure d'URL retenue. C'est un point bloquant distinct de l'arbitrage `/articles/` résolu ci-dessus.

Autres points non résolus, inchangés : `robots.txt` non vérifié (URL hors du périmètre autorisé pour l'outil de fetch de cette session) ; `site:optimasprotect.ma` à revérifier.

## Veille mots-clés & concurrents

- **Autocomplete (gl=ma, hl=fr)** sur les deux sujets prioritaires du backlog : « obligations loi 27-06 employeur » et « faux pointage agent de sécurité » → 0 suggestion pour les deux, y compris en mode étendu (alphabet soup) sur « logiciel gardiennage maroc ». Confirme un marché de recherche encore très étroit sur ces formulations précises, cohérent avec les runs précédents.
- **Google Trends (geo=MA, 12 mois)** sur « logiciel gardiennage maroc », « controle de ronde », « pointage agent securite » → signal quasi nul sur toute la période, un seul pic isolé (valeur 100, probablement un artefact de normalisation sur très faible volume) pour « controle de ronde » la semaine du 2025-11-02. Aucun intérêt régional mesurable sur les régions testées.
- **Concurrence :** pas de changement détecté par rapport au run du 2026-08-29 (SEKUR Africa toujours sans prix en dirhams publié pour le Maroc ; Trackforce Valiant toujours sur devis). Pas de re-vérification approfondie des pages concurrentes ce run, faute de nouveau signal déclencheur.

## Anti-doublon

9 branches `article/*` existantes, toutes en PR ouverte (voir tableau ci-dessous). Les deux sujets en tête de backlog (obligations Loi n°27-06 employeur ; faux pointage ADS) ont été vérifiés comme non couverts par un article existant — angles distincts de `cahier-des-charges-gardiennage-maroc` (obligations vs cahiers des charges) et de `application-pointage-ads-maroc` (détection d'anomalies vs description de l'outil).

| PR | Slug | Pilier | Statut |
|---|---|---|---|
| #1 | controle-de-ronde-nfc-gardiennage-maroc | 1 | ouverte, brouillon |
| #2 | prix-logiciel-gardiennage-maroc | 1 | ouverte, brouillon |
| #4 | main-courante-electronique-maroc | 2 | ouverte, brouillon |
| #5 | portail-client-securite-privee-maroc | 2 | ouverte, brouillon |
| #6 | planning-agents-securite-maroc | 3 | ouverte, brouillon |
| #7 | modele-rapport-de-ronde-maroc | 1 | ouverte, brouillon |
| #8 | cahier-des-charges-gardiennage-maroc | 1 | ouverte, brouillon (réglementaire, reste en PR en permanence) |
| #9 | application-pointage-ads-maroc | 1 | ouverte, brouillon |
| #10 | rapport-intervention-ads-maroc | 2 | ouverte, brouillon |

Répartition par pilier : pilier 1 = 5/9 (≈56 %), pilier 2 = 3/9 (≈33 %), pilier 3 = 1/9 (≈11 %) — proche du ratio cible 60/30/10.

## Scoring des sujets en file d'attente

| Sujet | Pilier | Intention | Faisabilité | Avantage local | Volume | Fraîcheur | Score /25 |
|---|---|---|---|---|---|---|---|
| Obligations Loi n°27-06 pour l'employeur | 1 | 4 | 4 | 5 | 2 | 3 | **18** |
| Faux pointage ADS : détecter sans accuser | 1/2 | 4 | 3 | 4 | 2 | 3 | **16** |

Les deux sujets dépassent le seuil de 14/25 et sont prêts à rédiger.

## Décision de production

**Aucun nouvel article produit aujourd'hui.** La raison d'incertitude sur `/articles/` qui avait motivé la pause du 2026-08-29 est levée, mais deux raisons distinctes justifient de maintenir la pause :

1. **Cadence hebdomadaire déjà largement dépassée.** La semaine S35 (24–30 août) a déjà produit 5 articles contre une cible de 2 à 3 par semaine en phase de lancement (§9).
2. **Neuf PR en attente de relecture humaine, aucune fusionnée à ce jour.** Ajouter un dixième article non relu ne sert pas la priorité « qualité avant quota » (§8.4).

Les deux sujets scorés ci-dessus restent en tête de file pour la semaine S36 (31 août – 6 septembre), en commençant par le sujet 1 (score 18/25, pilier 1, complète le hub conformité qui n'a qu'un seul article).

## Actions effectuées ce run

- Lecture intégrale de `.seo/agent-prompt.md` (v3.2) et de son historique de corrections.
- Vérification d'accès GitHub et cartographie du dépôt confirmée sans changement de structure.
- Spot-check du contenu d'un article en PR pour vérifier l'application des corrections `/articles/` et marque.
- Mise à jour de `.seo/repo-map.md` (commit direct sur `main`).
- Mise à jour de `.seo/backlog.md` (commit direct sur `main`).
- Veille mots-clés (autocomplete + Trends, marché Maroc) sur les deux sujets prioritaires du backlog.
- Rédaction de ce rapport.

## Résumé technique de fin de run

- **Moyen technique d'écriture GitHub :** connecteur MCP GitHub local (`local_unpacked_qunfei-wu_github-mcp-server-js`), appels API directs via ce connecteur.
- **Accès au dépôt :** fonctionnel (écriture confirmée sur `main`).
- **Articles produits / publiés / mis à jour ce run :** 0 produit, 0 publié (aucun mécanisme de publication live disponible — voir alerte haute), 2 fichiers de suivi mis à jour (`repo-map.md`, `backlog.md`).
- **Alerte haute :** absence de générateur de site connecté au dépôt — aucun article ne peut devenir une page live tant que ce point n'est pas résolu par un humain.
