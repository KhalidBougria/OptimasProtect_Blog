# Rapport de veille SEO — 2026-09-18

## Acces et bootstrap

Acces au depot confirme via le connecteur MCP GitHub local (local_unpacked_qunfei-wu_github-mcp-server-js), authentifie en tant que KhalidBougria (proprietaire du compte, pas obougria comme prevu au prompt de reference), permissions push: true / admin: true. Depot public cote API (et non prive comme l'indique le prompt de reference — ecart deja note les runs precedents, sans impact sur l'acces en ecriture).

Le prompt de reference .seo/agent-prompt.md (version 3.2) a ete relu integralement. Il prime sur le texte reproduit dans le corps de la tache planifiee elle-meme, qui divergeait encore sur trois points deja corriges cote depot : la loi applicable est la Loi n°27-06 relative aux activites de gardiennage et de transport de fonds (dahir n°1-07-155, 2007) et non « Loi n°35-09 » ; la structure d'URL des articles est /articles/{slug} et non /blog/{slug} ; l'orthographe de marque a utiliser pour la production future est OptimasProtect en un seul mot, et non « Optimas Protect » en deux mots. Ces trois corrections ont ete appliquees integralement ce run, conformement aux instructions de bootstrap.

Bash a ete indisponible tout le run (meme erreur de montage Windows documentee depuis le 8 septembre : "failed to mount ... outputs"). Contournement applique avec succes via le navigateur integre pour lire robots.txt et sitemap.xml en direct sur le site.

.seo/repo-map.md existait deja et etait a jour (derniere entree : 2026-09-16, soit 2 jours). Aucune reconstruction necessaire, seule une mise a jour incrementale.

## Sitemap et robots.txt (verifies en direct)

robots.txt inchange depuis le 2026-09-16 : User-agent: * / Allow: / plus la directive Sitemap, version simplifiee a deux lignes. Googlebot reste explicitement autorise. Aucune alerte.

sitemap.xml reconfirme : les 14 articles produits par ce pipeline sont toujours en ligne sous /articles/{slug}, plus le second canal independant /blog/main-courante-electronique-vs-papier toujours present et inchange (voir alerte non resolue plus bas).

## Veille mots-cles et SERP

Marche cible : Maroc (gl=ma, hl=fr). Rotation de 16 requetes sur des angles non testes lors des runs precedents (cumul depuis le 2026-08-22) : comment prouver une ronde de securite, rapport de ronde automatique, fiche de ronde de securite, cahier de presence agent de securite, gestion des rondes de nuit gardiennage, pointage agent de securite sans carte, solution digitale gardiennage PME Maroc, logiciel de gestion des interventions securite, alternative a Trackforce Maroc, cout d'un logiciel de gestion de securite privee, societe de securite avis client, KPI societe de securite privee, logiciel facturation societe de securite, indicateur de performance gardiennage, gestion du materiel agents de securite, convention collective gardiennage Maroc.

Deux suggestions obtenues sur 16, aucune exploitable :
- « fiche de ronde de securite » renvoie vers « rapport / exemple de rapport de ronde de securite », mele de bruit quebecois (SAAQ). Intention deja entierement couverte par la branche modele-rapport-de-ronde-maroc (PR 7, cluster 5).
- « indicateur de performance gardiennage » renvoie vers « indicateur de performance securite » generique (securite et sante au travail / HSE), hors sujet gardiennage prive.

trends_interest indisponible ce run (Google renvoie une erreur 429 sur le lot teste, geo=MA), coherent avec les interruptions intermittentes deja documentees certains jours. topic_demand_score sur « gestion du materiel agents de securite » : score 1/5, aucun signal exploitable sur les sources gratuites.

Regime pauvre en autocomplete confirme une nouvelle fois sur ce marche, conforme au constat repete depuis plusieurs semaines. Aucun sujet reellement neuf (pilier 1, 2 ou 3) n'a franchi le seuil de production de 14/25 ce run.

## Veille concurrentielle

SEKUR / SEKUR Africa (cercle 1, Maroc) : recherche web ce run confirme la tarification publique en EUR affichee directement sur sekur-africa.com (Pack Jeune Entreprise 69,99 EUR HT/mois, Pack Gestion 99,99 EUR HT/mois, Pack Terrain 9,99 EUR HT/mois), plus une page comparative "meilleurs logiciels securite privee 2026" egalement publiee sur sekur.fr (hors perimetre Maroc). Aucun changement de positionnement ou de prix detecte cote offre Maroc — l'opacite tarifaire en MAD reste confirmee, ce qui continue de renforcer le differenciateur prix en dirhams d'OptimasProtect.

EasyGard / SGGI (Marrakech) et Trackforce Valiant (international) : inchanges, aucune evolution de positionnement detectee.

KVER et SecuMall : reconfirmes lors de cette recherche comme revendeurs marocains de controleurs de ronde physiques (materiel RFID, pas logiciel SaaS) — cercle 3 (substituts au sens du paragraphe 2.3), pas des concurrents logiciels directs.

Aucun nouvel acteur logiciel marocain detecte ce run.

## Verification du depot

Les 14 branches article/* documentees dans .seo/backlog.md ont ete reconfirmees via list_branches (aucune branche supplementaire, aucune manquante). Le statut des pull requests a ete reverifie via list_pull_requests (etat closed) : seules les PR 3 et PR 11 (corrections du prompt de reference) sont fusionnees. Le depot compte donc toujours 14 pull requests d'articles ouvertes depuis le 2026-08-22, soit 27 jours au 2026-09-18, aucune fusionnee sur GitHub a ce jour.

## Decision de production

Aucun nouvel article produit ni mis a jour ce run. Les 10 clusters prioritaires du paragraphe 4.2 du prompt de reference restent tous couverts par au moins un article en PR ; la rotation de mots-cles n'a fait remonter aucun signal de demande exploitable non deja couvert ; le seuil de production de 14/25 n'a ete franchi par aucun sujet neuf. Conforme a la regle du paragraphe 4.1 (une journee sans article publie est normale, une journee sans veille ne l'est pas) et a la priorite qualite avant quota du paragraphe 8.4.

Repartition par pilier inchangee depuis le 2026-09-11 : pilier 1 = 8/14 (~57%), pilier 2 = 4/14 (~29%), pilier 3 = 2/14 (~14%), proche de la cible 60/30/10 du paragraphe 0. Aucune bascule de pilier necessaire ce run.

## Alertes

Alerte moyenne (reportee, non resolue depuis plusieurs runs) — file de pull requests sans relecture humaine. Le depot compte 14 PR d'articles ouvertes depuis 27 jours (2026-08-22 -> 2026-09-18), aucune fusionnee. Cette portee doit toutefois etre nuancee : selon les constats des 2026-09-12 et 2026-09-15 (non reverifies en profondeur ce run faute de nouvel element), le contenu des 14 articles etait deja visible sur le site public malgre l'absence de fusion, via un mecanisme de publication non identifie avec precision. Point a clarifier par l'utilisateur : ce mecanisme constitue-t-il deja la relecture humaine prevue en Phase 1, ou celle-ci doit-elle etre renforcee cote GitHub ?

Alerte moyenne (reportee, non resolue) — second canal de contenu independant. Un canal /blog/ distinct de ce depot continue de publier du contenu sur optimasprotect.ma (/blog/main-courante-electronique-vs-papier), toujours present au 2026-09-18, avec un risque de cannibalisation SEO sur le cluster main courante electronique deja traite par ce pipeline (main-courante-electronique-maroc, PR 4). Point a clarifier par l'utilisateur : qui gere ce canal, et faut-il l'integrer au perimetre de veille anti-doublon de cet agent ?

Alerte basse (reportee, sans impact) — ecart de branding sur l'habillage du site. Le site en direct affiche "OptImasProtect" (I majuscule median) dans la balise title et l'en-tete visuel, alors que le contenu editorial produit par cet agent utilise correctement "OptimasProtect" en un seul mot (convention confirmee le 2026-08-29). Ecart hors perimetre d'action de cet agent, a signaler a l'utilisateur.

Aucune alerte haute ce run : l'acces au depot fonctionne, le site reste indexable (Googlebot non restreint), et Search Console reste non connectee mais cette limitation etait deja connue et actee au bootstrap (paragraphe 9 du prompt de reference).

## Resume technique de fin de run

Moyen technique d'ecriture GitHub utilise : connecteur MCP GitHub local (API directe), authentifie KhalidBougria. Acces au depot : fonctionnel. Articles produits ce run : 0. Articles publies ce run : 0. Articles mis a jour ce run : 0. Alertes hautes : 0. Alertes moyennes : 2 (reportees). Alerte basse : 1 (reportee).
