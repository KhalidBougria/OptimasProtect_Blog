# Rapport de veille SEO & production éditoriale — 2026-09-05

## Bootstrap

- **Accès dépôt :** confirmé via appels directs à l'API GitHub (connecteur MCP local `local_unpacked_qunfei-wu_github-mcp-server-js`), authentifié en tant que `KhalidBougria` (propriétaire du compte) et non `obougria` comme prévu au prompt de référence. Permissions confirmées : `push: true`, `admin: true`. Visibilité du dépôt : `public` côté API (contrairement à l'hypothèse « privé » du prompt).
- **Prompt de référence :** `.seo/agent-prompt.md` (version 3.2) déjà présent, lu en entier. Deux corrections actives à retenir : la loi applicable est la **Loi n°27-06** (gardiennage et transport de fonds, dahir n°1-07-155, 2007) et non « Loi n°35-09 » ; l'URL cible du blog est `/articles/{slug}` et non `/blog/{slug}` ; la marque s'écrit **« OptimasProtect » en un seul mot** pour toute production future.
- **Cartographie du dépôt :** `.seo/repo-map.md` à jour, relue en entier. Dossier de contenu `content/blog/`, 12 branches `article/*` existantes avant ce run.
- **Search Console :** accès en lecture non configuré, conformément au bootstrap — veille appuyée sur SERP observées et autocomplete comme prévu.

## Veille du jour

**Mots-clés testés (autocomplete `gl=ma`, `hl=fr`) :**
- Pilier 2 : « SLA prestation gardiennage », « réclamation client société de sécurité », « litige client société de sécurité preuve », « indicateurs qualité prestation gardiennage » → 0 suggestion pour toutes.
- Pilier 3 : « carte professionnelle agent de sécurité Maroc », « congés agent de sécurité Maroc », « consigne de poste gardiennage modèle » → 0 suggestion pour toutes.
- Pilier 1 (adjacent) : « cahier de consignes sécurité privée » → 1 suggestion (« cahier de consignes sécurité ») ; « main courante sécurité privée » → 3 suggestions dont « main courante sécurité privée pdf » et « réglementation main courante sécurité privée » (intention déjà couverte par PR #4, pas de nouvel angle) ; « preuve de passage ronde » → 0 suggestion.
- **Trends (`geo=MA`, 12 mois)** sur trois requêtes pilier 2 : erreur 429 (Google Trends temporairement indisponible ce run).

**Recherche web complémentaire :** confirmation que SEKUR (sekur.fr) commercialise une fonctionnalité dédiée « Cahier de consignes » (`sekur.fr/cahier-de-consignes-logiciel-gestion-securite-privee/`, `sekur.fr/logiciel/terrain/cahier-de-consignes/`), validant l'intention business du sujet malgré un volume mesurable faible sur le marché marocain. Veille concurrentielle par ailleurs inchangée : SEKUR Africa toujours en tarification EUR opaque côté MAD, Trackforce Valiant toujours positionné à l'international, aucun nouvel acteur marocain détecté. `site:optimasprotect.ma` reconfirmé à 0 résultat pertinent indexé.

## Anti-doublon

Liste des 12 articles déjà en PR consultée avant sélection (voir `.seo/backlog.md`). Relecture complète du contenu de `article/main-courante-electronique-maroc` (PR #4) pour vérifier le chevauchement potentiel avec le sujet retenu ce jour : confirmé non-doublon (voir scoring ci-dessous).

## Scoring et sélection

| Sujet | Intention business | Faisabilité rank | Avantage local | Volume | Fraîcheur | **Total /25** |
|---|---|---|---|---|---|---|
| Cahier de consignes numérique (pilier 1) | 4 | 4 | 4 | 2 | 3 | **17** |

Seuil de production (14/25) atteint. Aucun sujet pilier 2 ou 3 réellement neuf n'a dépassé le seuil ce run (0 suggestion sur tous les angles testés) : **règle de bascule du §0 appliquée**, effort reporté sur le pilier 1. Le sujet retenu recoupe potentiellement les piliers 1 et 3 mais a été classé pilier 1 (traçabilité et preuve de transmission des consignes) en cohérence avec la priorité du §0.

## Production

**1 article produit :** *Cahier de consignes numérique pour agents de sécurité Maroc* (`cahier-de-consignes-securite-maroc`).
- Différencié explicitement de la main courante électronique (tableau comparatif dédié dans le corps), anti-doublon confirmé.
- Frontmatter complet avec `canonical` renseigné, `draft: true`, `author: "Team Optimas"`.
- 3 liens internes vérifiés sur fichiers réels : main courante électronique, prix logiciel, contrôle de ronde NFC.
- JSON-LD `SoftwareApplication` + `Offer` (99 / 179 MAD HT) et `FAQPage` (5 questions).
- Aucune affirmation réglementaire (article non réglementaire, pas de citation de loi nécessaire).
- Registre formel, vouvoiement, aucune image, aucun tiret cadratin, contre-point honnête inclus (« à qui ce modèle ne convient pas »).

## Publication

- Branche `article/cahier-de-consignes-securite-maroc` créée depuis `main`.
- Commit du fichier en `draft: true`.
- **Pull request #15** ouverte vers `main`, corps détaillant mot-clé focus, score, anti-doublon et contrôles passés. Reste en Phase 1 (relecture humaine requise avant fusion), conformément à la consigne de cette tâche planifiée.
- `.seo/backlog.md` et `.seo/repo-map.md` mis à jour (commit direct sur `main`, ce sont des documents de suivi et non des articles).

## Alertes

- **Alerte haute — 13 pull requests ouvertes depuis le 2026-08-22, aucune fusionnée** après plus de deux semaines. Signalé sans interruption depuis le 2026-08-31, continue de croître sans action humaine. Non bloquant pour la veille et la production, mais mérite un arbitrage.
- **Alerte haute — aucun générateur de site statique connecté à ce dépôt** (`.seo/repo-map.md`) : aucun article produit par cet agent ne peut devenir une page live tant qu'un humain n'a pas branché un générateur sur ce dépôt (ou relié le générateur existant du site `optimasprotect.ma`). Non résolu.
- `site:optimasprotect.ma` reconfirmé à 0 résultat pertinent indexé (recherche web).
- `robots.txt` toujours hors du périmètre autorisé pour l'outil de fetch de cette session, donc non vérifié directement.
- Google Trends indisponible ce run (erreur 429) sur les requêtes pilier 2 testées — à retenter la prochaine session.
- Ratio piliers désormais : pilier 1 = 8/13 (≈62 %), pilier 2 = 3/13 (≈23 %), pilier 3 = 2/13 (≈15 %). Pilier 2 reste sous la cible de 30 % ; priorité absolue au prochain run si un sujet pilier 2 franchit le seuil de 14/25.

## Résumé technique de fin de run

- **Moyen technique d'écriture GitHub :** appels directs à l'API GitHub via le connecteur MCP local `local_unpacked_qunfei-wu_github-mcp-server-js` (pas d'appel à un connecteur GitHub MCP distinct configuré côté Cowork).
- **Accès au dépôt :** fonctionnel (lecture et écriture confirmées).
- **Articles produits :** 1 (`cahier-de-consignes-securite-maroc`, PR #15, pilier 1, score 17/25, en attente de relecture).
- **Articles publiés (mergés) :** 0 — Phase 1 maintenue, aucune fusion effectuée par l'agent.
- **Articles mis à jour :** 0.
- **Alertes hautes :** 2 (PR non relues en nombre croissant ; absence de générateur de site connecté).
